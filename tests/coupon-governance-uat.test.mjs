import test from"node:test";
import assert from"node:assert/strict";
import fs from"node:fs";
const read=path=>fs.readFileSync(new URL(`../${path}`,import.meta.url),"utf8");
const governance=read("lib/coupon-governance.ts"),route=read("app/api/coupon-governance/route.ts"),client=read("lib/coupon-governance-client.ts"),legacy=read("lib/offer-engine.ts");

test("coupon commercial truth is persisted on the server",()=>{assert.match(governance,/coupon_campaigns/);assert.match(governance,/coupon_quotes/);assert.match(governance,/coupon_redemptions/);assert.match(governance,/policy_snapshot_json/);assert.match(governance,/testOnly:true/);});
test("server derives customer eligibility and enforces campaign limits",()=>{assert.match(governance,/SELECT COUNT\(\*\) count FROM canonical_bookings WHERE customer_id/);assert.match(governance,/customer_grooming_subscriptions/);assert.match(governance,/perCustomerLimit/);assert.match(governance,/totalLimit/);assert.match(governance,/Coupon is outside its validity window/);assert.match(governance,/Coupon is not eligible for this service/);assert.match(governance,/Coupon is not eligible in this city/);assert.match(governance,/Coupon is not eligible on this channel/);});
test("server computes bounded discounts instead of trusting browser totals",()=>{assert.match(governance,/Math\.round\(input\.orderValue\*campaign\.discountValue\/100\)/);assert.match(governance,/Math\.max\(0,Math\.min\(raw,campaign\.maxDiscount\?\?raw,input\.orderValue\)\)/);assert.doesNotMatch(client,/discountAmount:/);});
test("coupon quotes freeze policy and expire before redemption",()=>{assert.match(governance,/expiresAt=now\+15\*60_000/);assert.match(governance,/policySnapshot:snapshot/);assert.match(governance,/Coupon quote has expired/);});
test("redemption is canonical-booking bound and idempotent",()=>{assert.match(governance,/idempotency_key TEXT NOT NULL UNIQUE/);assert.match(governance,/booking_id TEXT NOT NULL UNIQUE/);assert.match(governance,/SELECT id,customer_id,status FROM canonical_bookings/);assert.match(governance,/duplicatePrevented:true/);});
test("coupon API separates customer quote from staff management and consumption",()=>{assert.match(route,/action==="quote"/);assert.match(route,/requirePermission\(actor,"scheduling\.book"\)/);assert.match(route,/requireCustomerOwnership/);assert.match(route,/action==="save_campaign"/);assert.match(route,/requirePermission\(actor,"pricing\.manage"\)/);assert.match(route,/action==="consume"/);assert.match(route,/requirePermission\(actor,"bookings\.manage"\)/);assert.match(route,/productionReady:false/);});
test("legacy browser engine is documented as non-authoritative by the new boundary",()=>{assert.match(legacy,/offerStorageKey/);assert.match(client,/\/api\/coupon-governance/);assert.doesNotMatch(route,/readOfferConfig|localStorage|validateOffer/);});
