import test from "node:test";
import assert from "node:assert/strict";
import { DatabaseSync } from "node:sqlite";
import * as nodeModule from "node:module";
const WORKERS_SHIM = `export const env = new Proxy({}, { get: (_, key) => globalThis.__PAWSPACE_TEST_ENV?.[key] });`;
const workersUrl = `data:text/javascript,${encodeURIComponent(WORKERS_SHIM)}`;
if (typeof nodeModule.registerHooks === "function") { nodeModule.registerHooks({resolve(specifier, context, nextResolve) {if (specifier === "cloudflare:workers") return { url: workersUrl, shortCircuit: true };try { return nextResolve(specifier, context); }catch (e) { if (specifier.startsWith(".") && !specifier.endsWith(".ts")) return nextResolve(`${specifier}.ts`, context); throw e; }}}); } else { const hook = `const workersUrl=${JSON.stringify(workersUrl)}; export async function resolve(specifier, context, nextResolve) {if (specifier === "cloudflare:workers") return { url: workersUrl, shortCircuit: true };try { return await nextResolve(specifier, context); }catch (e) { if (specifier.startsWith(".") && !specifier.endsWith(".ts")) return nextResolve(specifier + ".ts", context); throw e; }}`; nodeModule.register(new URL(`data:text/javascript,${encodeURIComponent(hook)}`)); }
function makeD1(sqlite) {function statement(sql, args) {return {bind: (...b) => statement(sql, b),first: async () => { const r = sqlite.prepare(sql).get(...args); return r === undefined ? null : r; },run: async () => { const i = sqlite.prepare(sql).run(...args); return { success: true, meta: { changes: Number(i.changes) } }; },all: async () => ({ results: sqlite.prepare(sql).all(...args) })};}return { prepare: (sql) => statement(sql, []), batch: async (list) => { const out = []; for (const s of list) out.push(await s.run()); return out; }, exec: async (sql) => { sqlite.exec(sql); } };}
const PERIOD = "2026-07", ACTOR = "maker@pawspace.in";const IST = 330 * 60_000, istMs = (y, m, d) => Date.UTC(y, m - 1, d) - IST;
async function world() {const sqlite = new DatabaseSync(":memory:");const db = makeD1(sqlite);globalThis.__PAWSPACE_TEST_ENV = { DB: db };sqlite.exec(`CREATE TABLE provider_commercial_terms (id TEXT PRIMARY KEY, engagement_model TEXT);CREATE TABLE provider_payout_computations (booking_id TEXT PRIMARY KEY, provider_id TEXT, service_code TEXT, order_value REAL, provider_net_payout REAL, provider_gst_deducted REAL, term_id TEXT, computed_at INTEGER);CREATE TABLE canonical_bookings (id TEXT PRIMARY KEY, customer_id TEXT NOT NULL, city_id TEXT NOT NULL);`);return { sqlite, db };}
function term(sqlite, id, model) { sqlite.prepare("INSERT INTO provider_commercial_terms (id,engagement_model) VALUES (?,?)").run(id, model); }
function payout(sqlite, { booking, provider, order, gst, term, at, net = 0, city = "blr" }) {sqlite.prepare("INSERT OR IGNORE INTO canonical_bookings (id,customer_id,city_id) VALUES (?,?,?)").run(booking, `CUS-${booking}`, city);sqlite.prepare("INSERT INTO provider_payout_computations (booking_id,provider_id,service_code,order_value,provider_net_payout,provider_gst_deducted,term_id,computed_at) VALUES (?,?,?,?,?,?,?,?)").run(booking, provider, "boarding", order, net, gst, term, at);}
test("s52 TCS engine collects current 0.5% of net value on marketplace supplies only, split CGST+SGST", async () => {const { sqlite, db } = await world();const tcs = await import("../lib/tcs-governance.ts");term(sqlite, "T_mkt", "commission_standard");term(sqlite, "T_emp", "direct_employee");payout(sqlite, { booking: "bk1", provider: "P1", order: 1180, gst: 180, term: "T_mkt", at: istMs(2026, 7, 10) });payout(sqlite, { booking: "bk2", provider: "P1", order: 590, gst: 90, term: "T_mkt", at: istMs(2026, 7, 20) });payout(sqlite, { booking: "bk3", provider: "E9", order: 2000, gst: 0, term: "T_emp", at: istMs(2026, 7, 11) });payout(sqlite, { booking: "bk4", provider: "P2", order: 1180, gst: 180, term: "T_mkt", at: istMs(2026, 6, 28) });const res = await tcs.computeMonthlyTcs(db, { period: PERIOD, actorId: ACTOR });assert.equal(res.supplierCount, 1);assert.equal(res.totalNetValue, 1500);assert.equal(res.totalTcs, 7.5);assert.equal(res.cgstTcs, 3.75);assert.equal(res.sgstTcs, 3.75);assert.equal(sqlite.prepare("SELECT COUNT(*) n FROM tcs_collections WHERE period=?").get(PERIOD).n, 2);assert.deepEqual(sqlite.prepare("SELECT DISTINCT rate_pct FROM tcs_collections WHERE period=?").all(PERIOD).map(r=>r.rate_pct), [0.5]);const gstr8 = await tcs.prepareGstr8(db, { period: PERIOD, actorId: ACTOR });assert.equal(gstr8.totalTcs, 7.5);assert.equal(gstr8.liveFilingEnabled, false);await tcs.recordTcsDeposit(db, { period: PERIOD, challanReference: "CH-1", amount: 7.5, actorId: ACTOR });});
test("s52 place of supply decides tax heads: intra uses CGST+SGST and inter uses IGST", async () => {const { sqlite, db } = await world();const tcs = await import("../lib/tcs-governance.ts");term(sqlite, "T_mkt", "commission_standard");payout(sqlite, { booking: "bk_blr", provider: "P1", order: 1180, gst: 180, term: "T_mkt", at: istMs(2026, 7, 10) });payout(sqlite, { booking: "bk_bom", provider: "P2", order: 1180, gst: 180, term: "T_mkt", at: istMs(2026, 7, 11), city: "mumbai" });const res = await tcs.computeMonthlyTcs(db, { period: PERIOD, actorId: ACTOR });assert.equal(res.totalNetValue, 2000);assert.equal(res.totalTcs, 10);assert.equal(res.cgstTcs, 2.5);assert.equal(res.sgstTcs, 2.5);assert.equal(res.igstTcs, 5);const heads = sqlite.prepare("SELECT booking_id,supply_type,cgst_tcs,sgst_tcs,igst_tcs FROM tcs_collections WHERE period=? ORDER BY booking_id").all(PERIOD);assert.deepEqual(heads.map((r) => [r.booking_id, r.supply_type]), [["bk_blr", "intra"], ["bk_bom", "inter"]]);});
test("s52 rate is effective-dated across 10 July 2024 without rewriting history", async () => {const { tcsRateS52For } = await import("../lib/tcs-governance.ts");assert.equal(tcsRateS52For(istMs(2024, 7, 9)).total, 0.01);assert.equal(tcsRateS52For(istMs(2024, 7, 10)).total, 0.005);assert.equal(tcsRateS52For(istMs(2024, 7, 10)).cgst, 0.0025);assert.equal(tcsRateS52For(istMs(2024, 7, 10)).sgst, 0.0025);assert.equal(tcsRateS52For(istMs(2024, 7, 10)).igst, 0.005);});
test("computeMonthlyTcs is idempotent", async () => {const { sqlite, db } = await world();const tcs = await import("../lib/tcs-governance.ts");term(sqlite, "T_mkt", "commission_standard");payout(sqlite, { booking: "bk1", provider: "P1", order: 1180, gst: 180, term: "T_mkt", at: istMs(2026, 7, 10) });await tcs.computeMonthlyTcs(db, { period: PERIOD, actorId: ACTOR });await tcs.computeMonthlyTcs(db, { period: PERIOD, actorId: ACTOR });assert.equal(sqlite.prepare("SELECT COUNT(*) n FROM tcs_collections WHERE period=?").get(PERIOD).n, 1);});
test("TDS reconciliation flags mismatch, PAN-pending, and deposit gaps", async () => {const { sqlite, db } = await world();const tds = await import("../lib/tds-governance.ts");const recon = await import("../lib/tds-tcs-reconciliation.ts");await tds.ensureTdsTables(db);const insert = (section, id, base, rate, amount, pan) => sqlite.prepare("INSERT INTO tds_deductions (id,period,section,deductee_type,deductee_id,deductee_name,pan_status,base_amount,rate_pct,tds_amount,source_type,source_ref,computed_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?)").run(`d_${id}`, PERIOD, section, "provider", id, id, pan, base, rate, amount, "provider_payouts", id, Date.now());insert("194J", "P_ok", 1000, 10, 100, "verified");insert("194H", "P_bad", 1000, 2, 50, "verified");insert("194H", "P_pan", 1000, 2, 20, "pending_verification");const res = await recon.reconcileTds(db, { period: PERIOD });assert.equal(res.summary.recordedTds, 170);assert.ok(res.findings.some((f) => f.code === "amount_mismatch" && f.deducteeId === "P_bad"));assert.ok(res.findings.some((f) => f.code === "pan_pending" && f.deducteeId === "P_pan"));assert.equal(res.summary.depositStatus, "missing");assert.equal(res.summary.reconciled, false);});
test("TCS reconciliation verifies current-rate collections and catches tampering", async () => {const { sqlite, db } = await world();const tcs = await import("../lib/tcs-governance.ts");const recon = await import("../lib/tds-tcs-reconciliation.ts");term(sqlite, "T_mkt", "commission_standard");payout(sqlite, { booking: "bk1", provider: "P1", order: 1180, gst: 180, term: "T_mkt", at: istMs(2026, 7, 10) });await tcs.computeMonthlyTcs(db, { period: PERIOD, actorId: ACTOR });await tcs.recordTcsDeposit(db, { period: PERIOD, challanReference: "CH-1", amount: 5, actorId: ACTOR });const clean = await recon.reconcileTcs(db, { period: PERIOD });assert.equal(clean.summary.reconciled, true);assert.equal(clean.summary.recordedTcs, 5);sqlite.prepare("UPDATE tcs_collections SET tcs_total=99 WHERE period=?").run(PERIOD);const tampered = await recon.reconcileTcs(db, { period: PERIOD });assert.equal(tampered.summary.reconciled, false);assert.ok(tampered.findings.some((f) => f.code === "amount_mismatch"));});
