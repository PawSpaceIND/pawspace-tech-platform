import test from 'node:test';
import assert from 'node:assert/strict';
import {walkingQuoteNeedsReview} from '../lib/walking-quote-consent.ts';
const quote={quoteId:'original',packageCode:'walking-30',packageName:'30-minute Solo Walk',packageVersion:1,durationMinutes:30,mode:'recurring',petCount:1,walkCount:2,weekdays:[1,3],scheduledStart:'2026-09-10T01:30:00Z',scheduledEnd:'2026-09-10T02:00:00Z',perWalkAmount:349,totalAmount:698,amountDueNow:0,paymentMode:'pay_after_service',expiresAt:1000,liveMoney:false};
test('Walking requires another review for any changed price or service terms',()=>{for(const change of [{packageCode:'walking-60'},{mode:'once'},{petCount:2},{paymentMode:'prepay'},{liveMoney:true},{scheduledEnd:'2026-09-11T02:00:00Z'},{totalAmount:798},{totalAmount:598},{perWalkAmount:399},{amountDueNow:100},{packageVersion:2},{packageName:'Changed package'},{durationMinutes:60},{walkCount:3},{weekdays:[2,4]},{scheduledStart:'2026-09-11T01:30:00Z'}])assert.equal(walkingQuoteNeedsReview(quote,{...quote,...change}),true,JSON.stringify(change));});
test('refreshing quote identity and expiry does not force an endless confirmation loop',()=>{assert.equal(walkingQuoteNeedsReview(quote,{...quote,quoteId:'refreshed',expiresAt:2000,weekdays:[3,1]}),false);});
test('an unseen quote needs review; a reviewed updated price can then be confirmed',()=>{const updated={...quote,totalAmount:798,perWalkAmount:399};assert.equal(walkingQuoteNeedsReview(null,updated),true);assert.equal(walkingQuoteNeedsReview(quote,updated),true);assert.equal(walkingQuoteNeedsReview(updated,{...updated,quoteId:'next'}),false);});
