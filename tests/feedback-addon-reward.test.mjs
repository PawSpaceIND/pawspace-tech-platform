import test from"node:test";
import assert from"node:assert/strict";
import{freshCountingD1}from"./helpers/d1-harness.mjs";
import{installWorkersHooks}from"./helpers/module-hooks.mjs";
installWorkersHooks("__FEEDBACK_ADDON_DB__");
const config=await import("../lib/review-configuration-governance.ts");
const reviews=await import("../lib/service-review-governance.ts");
test("backend can switch feedback reward from Rs.500 coupon to one free configured add-on",async()=>{const ctx=freshCountingD1();globalThis.__FEEDBACK_ADDON_DB__=ctx.db;ctx.sqlite.exec("CREATE TABLE canonical_bookings (id TEXT PRIMARY KEY,customer_id TEXT,provider_id TEXT,service_code TEXT,status TEXT,package_name TEXT,scheduled_start TEXT)");ctx.sqlite.prepare("INSERT INTO canonical_bookings VALUES ('B','C','P','grooming','completed','Bath','2026-09-01')").run();await reviews.ensureServiceReviewTables(ctx.db);const draft=await config.saveReviewConfig(ctx.db,{serviceCode:"grooming",questions:[],triggerType:"every_service",channels:["notification"],feedbackRewardKind:"addon",feedbackAddonCode:"NAIL_TRIM"},"maker@pawspace.in");await config.approveReviewConfig(ctx.db,{id:draft.id,approvalReference:"TEST-ADDON",actor:"checker@pawspace.in"});const req=await reviews.requestServiceReview(ctx.db,{bookingId:"B",serviceCode:"grooming",customerId:"C"});const answers=Object.fromEntries(req.questions.map(q=>[q.id,5]));const result=await reviews.submitServiceReview(ctx.db,{requestId:req.requestId,customerId:"C",answers});assert.equal(result.feedbackReward.kind,"addon");assert.equal(result.feedbackReward.discount,0);assert.equal(result.feedbackReward.addonCode,"NAIL_TRIM");const redeemed=await reviews.redeemReviewReward(ctx.db,{code:result.feedbackReward.code,customerId:"C",bookingId:"B",actorId:"C"});assert.equal(redeemed.addOnCode,"NAIL_TRIM");assert.equal(redeemed.discountApplied,0);});
