// Playwright config discovers tests/e2e/**/*.spec.ts. Keep the requested proof source
// at tests/founder-uat-journey.spec.ts and register it here without duplicating logic.
import "../founder-uat-journey.spec";
