type Row=Record<string,unknown>;const text=(v:unknown)=>String(v??"").trim();
export async function ensurePartnerShiftLivenessTables(db:D1Database){await db.batch([
 db.prepare("CREATE TABLE IF NOT EXISTS partner_shift_liveness_checks (id TEXT PRIMARY KEY,provider_id TEXT NOT NULL,application_id TEXT,shift_date TEXT NOT NULL,idfy_reference TEXT,status TEXT NOT NULL,match_score REAL,checked_at INTEGER NOT NULL,expires_at INTEGER NOT NULL,detail_json TEXT NOT NULL DEFAULT '{}',UNIQUE(provider_id,shift_date))"),
 db.prepare("CREATE INDEX IF NOT EXISTS idx_partner_shift_liveness_provider ON partner_shift_liveness_checks(provider_id,shift_date,status)")]);}
export async function assertPartnerDailyScheduleLiveness(db:D1Database,input:{providerId:string;at?:number;enforce?:boolean}){await ensurePartnerShiftLivenessTables(db);const now=input.at??Date.now(),day=new Date(now+330*60_000).toISOString().slice(0,10);if(input.enforce===false)return{required:false,matched:false,shiftDate:day,applicationId:null,checkId:null};const app=await db.prepare("SELECT id,verification_status FROM provider_onboarding_applications WHERE provider_id=? ORDER BY updated_at DESC LIMIT 1").bind(input.providerId).first<Row>().catch(()=>null);if(!app||text(app.verification_status)!=="verified")throw new Response("Verified IDfy onboarding identity is required before shift start",{status:409});const check=await db.prepare("SELECT * FROM partner_shift_liveness_checks WHERE provider_id=? AND shift_date=? AND status='matched' AND expires_at>?").bind(input.providerId,day,now).first<Row>();if(!check)throw Response.json({error:"shift_liveness_required",message:"Complete a live selfie/liveness match against your verified onboarding profile before viewing today's schedule.",providerId:input.providerId,shiftDate:day},{status:428});return{required:true,matched:Boolean(check),shiftDate:day,applicationId:text(app.id),checkId:check?text(check.id):null};}
export async function recordPartnerShiftLivenessResult(db:D1Database,input:{providerId:string;applicationId:string;idfyReference:string;matched:boolean;matchScore?:number;detail?:unknown;at?:number}){await ensurePartnerShiftLivenessTables(db);const now=input.at??Date.now(),day=new Date(now+330*60_000).toISOString().slice(0,10),id=`LIVENESS-${crypto.randomUUID().slice(0,12).toUpperCase()}`;await db.prepare("INSERT INTO partner_shift_liveness_checks (id,provider_id,application_id,shift_date,idfy_reference,status,match_score,checked_at,expires_at,detail_json) VALUES (?,?,?,?,?,?,?,?,?,?) ON CONFLICT(provider_id,shift_date) DO UPDATE SET application_id=excluded.application_id,idfy_reference=excluded.idfy_reference,status=excluded.status,match_score=excluded.match_score,checked_at=excluded.checked_at,expires_at=excluded.expires_at,detail_json=excluded.detail_json").bind(id,input.providerId,input.applicationId,input.idfyReference,input.matched?"matched":"failed",input.matchScore??null,now,now+18*60*60_000,JSON.stringify(input.detail??{})).run();return{providerId:input.providerId,shiftDate:day,status:input.matched?"matched":"failed",idfyReference:input.idfyReference};}
