import type {AtlasBusinessSnapshot} from "./atlas-business-snapshot";
import {buildAtlasProposalFreshness} from "./atlas-proposal-freshness";
import {buildAtlasChallengeReview} from "./atlas-challenge-review";
import {buildAtlasDecisionQuality} from "./atlas-decision-quality";
import {buildAtlasRecommendationConsistency} from "./atlas-recommendation-consistency";
type Db=D1Database;
export type AtlasPrecedentReadiness={proposalId:string;proposalType:string;status:string;readiness:"ready_for_human_reuse_review"|"not_ready";score:number|null;fresh:boolean;terminal:boolean;challengeCount:number;consistentReplay:boolean|null;blockers:string[];requiresFounderReview:true;advisoryOnly:true;authorityMutationAllowed:false;causalOutcomeUsed:false;note:string};
export async function buildAtlasPrecedentReadiness(db:Db,snapshot:AtlasBusinessSnapshot,limit=20):Promise<AtlasPrecedentReadiness[]>{
 const [freshness,challenges,quality,consistency]=await Promise.all([buildAtlasProposalFreshness(db,snapshot,limit),buildAtlasChallengeReview(db,snapshot,limit),buildAtlasDecisionQuality(db,limit),buildAtlasRecommendationConsistency(db,limit)]),challengeById=new Map(challenges.map(x=>[x.proposalId,x])),qualityById=new Map(quality.map(x=>[x.proposalId,x])),consistencyById=new Map<string,boolean>();
 for(const group of consistency)for(const proposalId of group.proposalIds)consistencyById.set(proposalId,group.consistent);
 return freshness.map(f=>{const c=challengeById.get(f.proposalId),q=qualityById.get(f.proposalId),consistentReplay=consistencyById.has(f.proposalId)?consistencyById.get(f.proposalId)!:null,blockers:string[]=[];if(!f.fresh)blockers.push(f.reason);if(f.terminal)blockers.push("proposal_terminal_state");if((c?.challengeCount??0)>0)blockers.push("challenge_review_has_open_findings");if(consistentReplay===false)blockers.push("exact_evidence_replay_inconsistent");if(!q)blockers.push("decision_quality_unavailable");else if(q.score<75)blockers.push("decision_quality_below_review_threshold");const ready=blockers.length===0;return{proposalId:f.proposalId,proposalType:f.proposalType,status:f.status,readiness:ready?"ready_for_human_reuse_review" as const:"not_ready" as const,score:q?.score??null,fresh:f.fresh,terminal:f.terminal,challengeCount:c?.challengeCount??0,consistentReplay,blockers,requiresFounderReview:true as const,advisoryOnly:true as const,authorityMutationAllowed:false as const,causalOutcomeUsed:false as const,note:ready?"Proposal meets the evidence and process prerequisites for Founder reuse review only. It is not auto-approved or automatically reusable.":"Proposal is not precedent-ready. Resolve the listed blockers and re-evaluate under current canonical evidence before human review only; Atlas does not grant reuse authority."};}).sort((a,b)=>Number(a.readiness==="ready_for_human_reuse_review")-Number(b.readiness==="ready_for_human_reuse_review")||(b.score??-1)-(a.score??-1));
}
