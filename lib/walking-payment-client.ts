export type WalkingSandboxCapture={quoteId:string;status:"captured";amount:number;currency:string;environment:"sandbox";reference:string;duplicatePrevented:boolean;liveMoney:false;synthetic:true};
export async function captureWalkingQuoteSandbox(input:{quoteId:string;amount:number}){const response=await fetch("/api/walking-payment-sandbox",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(input)});const body=await response.json() as {data?:WalkingSandboxCapture;error?:string};if(!response.ok||!body.data)throw new Error(body.error??"Walking sandbox capture failed");return body.data;}
