export type TrainingCancellationRequest={caseId:string;status:string;calculation?:{calculatedRefund:number;capturedAmount:number;chargeableSessions:number;outstandingServiceValue:number}|null;duplicatePrevented:boolean;liveRefund:false};
export async function requestTrainingCancellation(input:{bookingId:string;reason:string}){const response=await fetch("/api/training-cancellation",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"request",bookingId:input.bookingId,reason:input.reason,idempotencyKey:`training-cancel:${input.bookingId}`})}),body=await response.json() as {data?:TrainingCancellationRequest;error?:string};if(!response.ok||!body.data)throw new Error(body.error||"Unable to request Training cancellation");return body.data;}
