export type BoardingPackage={package_code:string;name:string;care_kind:"daycare"|"overnight";max_hours:number;base_price_per_pet:number;currency:string;max_pets:number;version:number};
export type BoardingHost={providerId:string;name:string;model:"full_time"|"commission";area:string;rating:number;qualityScore:number;capacity:number;species:string[];oneFamilyOnly:boolean;medicationSupport:boolean;residentPets:string;homeVerified:boolean;kycStatus:string;backgroundCheckStatus:string;profileVersion:number};
export type BoardingQuote={quoteId:string;packageCode:string;packageName:string;packageVersion:number;petCount:number;scheduledStart:string;scheduledEnd:string;durationHours:number;stayUnits:number;basePricePerPet:number;totalAmount:number;amountDueNow:number;paymentMode:"prepaid";expiresAt:number;liveMoney:false};

async function payload<T>(response:Response){const body=await response.json() as {data?:T;error?:string};if(!response.ok||!body.data)throw new Error(body.error||"Boarding request failed");return body.data;}
export async function loadBoardingCommercial(input:{cityId:string;zoneId:string}){return payload<{packages:BoardingPackage[];hosts:BoardingHost[];source:string;liveAvailability:false;liveMoney:false}>(await fetch(`/api/boarding-commercial?cityId=${encodeURIComponent(input.cityId)}&zoneId=${encodeURIComponent(input.zoneId)}`,{cache:"no-store"}));}
export async function quoteBoarding(input:{packageCode:string;petCount:number;scheduledStart:string;scheduledEnd:string;paymentMode:"prepaid";couponCode?:string}){return payload<BoardingQuote>(await fetch("/api/boarding-commercial",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(input)}));}
