export async function acceptWalkingReplacement(input:{bookingId:string}){const response=await fetch("/api/walking-recovery",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({bookingId:input.bookingId,idempotencyKey:`walking-recovery-accept:${input.bookingId}`})});const body=await response.json() as {data?:Record<string,unknown>;error?:string};if(!response.ok||!body.data)throw new Error(body.error??"Unable to accept Dog Walking replacement");return body.data}
