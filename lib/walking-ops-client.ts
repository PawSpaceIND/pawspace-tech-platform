export async function loadWalkingOpsQueue(){const response=await fetch("/api/walking-ops",{cache:"no-store"});const body=await response.json() as {data?:{items:Array<Record<string,unknown>>};error?:string};if(!response.ok||!body.data)throw new Error(body.error??"Walking Operations queue could not be loaded");return body.data.items;}
export async function loadWalkingRecoveryCandidates(bookingId:string){const response=await fetch(`/api/walking-ops?bookingId=${encodeURIComponent(bookingId)}&candidates=1`,{cache:"no-store"});const body=await response.json() as {data?:{bookingId:string;candidates:Array<{id:string;name:string;model:"full_time"|"commission"}>};error?:string};if(!response.ok||!body.data)throw new Error(body.error??"Walking recovery candidates could not be loaded");return body.data.candidates;}
export async function updateWalkingOps(input:{bookingId:string;action:"assign_replacement"|"close_recovery"|"add_note";idempotencyKey:string;providerId?:string;note?:string}){const response=await fetch("/api/walking-ops",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(input)});const body=await response.json() as {data?:Record<string,unknown>;error?:string};if(!response.ok||!body.data)throw new Error(body.error??"Walking Operations action failed");return body.data;}
