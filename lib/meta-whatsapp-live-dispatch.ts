import{centralConsentAllows,reserveCommunicationFrequency}from"./communication-governance";
import{deadLetterOutbox,enforceCommunicationDispatchPolicy,ensureCommunicationTables,failOutboxAttempt}from"./communication-engine";
import{recordAtomicDeliveryEvent}from"./communication-delivery-state";
import{resolveCanonicalRecipientOwnership}from"./canonical-recipient-ownership";
import{buildMetaWhatsAppRequest}from"./meta-whatsapp-uat-dispatch";
type Db=D1Database;type Env=Record<string,unknown>;type Row=Record<string,unknown>;type Fetcher=(input:string|URL,init?:RequestInit)=>Promise<Response>;const text=(v:unknown)=>String(v??"").trim();
function graphVersion(env:Env){const value=text(env.META_WHATSAPP_GRAPH_VERSION)||"v23.0";if(!/^v\d+\.\d+$/.test(value))throw new Error("Meta Graph API version is invalid");return value;}
function url(env:Env,id:string){return`https://graph.facebook.com/${graphVersion(env)}/${encodeURIComponent(id)}/messages`;}
async function consent(db:Db,customerId:string){const row=await db.prepare("SELECT whatsapp_consent,opt_out FROM customer_contact_preferences WHERE customer_id=?").bind(customerId).first<Row>().catch(()=>null);return Boolean(row)&&Number(row!.whatsapp_consent)===1&&Number(row!.opt_out||0)===0&&await centralConsentAllows(db,customerId,"whatsapp");}
export async function dispatchMetaWhatsAppLive(db:Db,env:Env,input:{messageId:string;recipient?:string|null;fetcher?:Fetcher}){await ensureCommunicationTables(db);if(text(env.PAWSPACE_DEPLOYMENT_ENV).toLowerCase()!=="production"||text(env.PAWSPACE_COMMUNICATION_ENV).toLowerCase()!=="live")return{status:"not_live",provider:"meta_whatsapp",externalDelivery:false,productionDelivery:false};const token=text(env.META_WHATSAPP_ACCESS_TOKEN),phoneId=text(env.META_WHATSAPP_PHONE_NUMBER_ID);if(!token||!phoneId)return{status:"not_configured",provider:"meta_whatsapp",externalDelivery:false,productionDelivery:false};const message=await db.prepare("SELECT m.*,o.status outbox_status,o.next_attempt_at FROM communication_messages m JOIN communication_outbox o ON o.message_id=m.id WHERE m.id=?").bind(input.messageId).first<Row>();if(!message||text(message.channel)!=="whatsapp")throw new Error("Queued WhatsApp communication message not found");if(!["queued","retry_pending","scheduled"].includes(text(message.outbox_status)))return{status:"already_dispatched",provider:"meta_whatsapp",externalDelivery:false,productionDelivery:false};if(Number(message.next_attempt_at)>Date.now())return{status:"scheduled",provider:"meta_whatsapp",externalDelivery:false,productionDelivery:false};const owner=await resolveCanonicalRecipientOwnership(db,env,{phone:text(input.recipient)||null,customerId:text(message.customer_id),leadId:text(message.lead_id)||null,bookingId:text(message.booking_id)||null,requireSuppliedPhone:Boolean(text(input.recipient))});if(!await consent(db,owner.customerId))return{status:"consent_refused",provider:"meta_whatsapp",externalDelivery:false,productionDelivery:false};let payload:Row={};try{payload=JSON.parse(text(message.payload_json)||"{}") as Row;}catch{return{...(await deadLetterOutbox(db,input.messageId,"invalid_meta_payload")),provider:"meta_whatsapp",externalDelivery:false,productionDelivery:false};}const templateKey=text(message.template_key),language=text(payload.language)||"en",approved=await db.prepare("SELECT status,approved_language FROM whatsapp_uat_templates WHERE template_key=?").bind(templateKey).first<Row>().catch(()=>null);if(!approved||text(approved.status)!=="approved"||text(approved.approved_language)!==language)return{...(await deadLetterOutbox(db,input.messageId,"meta_template_not_approved")),provider:"meta_whatsapp",externalDelivery:false,productionDelivery:false};const policy=await enforceCommunicationDispatchPolicy(db,input.messageId,Date.now());if(!policy.allowed)return{...policy,provider:"meta_whatsapp",externalDelivery:false,productionDelivery:false};const frequency=await reserveCommunicationFrequency(db,{customerId:owner.customerId,channel:"whatsapp",messageId:input.messageId});if(!frequency.allowed)return{status:"frequency_capped",provider:"meta_whatsapp",nextEligibleAt:frequency.releaseAt,externalDelivery:false,productionDelivery:false};const claim=await db.prepare("UPDATE communication_outbox SET status='dispatching',locked_at=?,updated_at=? WHERE message_id=? AND status IN ('queued','retry_pending','scheduled') AND next_attempt_at<=?").bind(Date.now(),Date.now(),input.messageId,Date.now()).run();if(!Number(claim.meta?.changes||0))return{status:"dispatch_claim_lost",provider:"meta_whatsapp",externalDelivery:false,productionDelivery:false};const requestBody=buildMetaWhatsAppRequest({recipient:owner.dialNumber,templateKey,language,messageText:text(payload.text),withinSession:false}),controller=new AbortController(),timer=setTimeout(()=>controller.abort(),10_000);let response:Response;try{response=await(input.fetcher??fetch)(url(env,phoneId),{method:"POST",redirect:"error",signal:controller.signal,headers:{authorization:`Bearer ${token}`,"content-type":"application/json","x-pawspace-idempotency-key":text(message.idempotency_key)},body:JSON.stringify(requestBody)});}catch(error){clearTimeout(timer);const reason=error instanceof Error&&error.name==="AbortError"?"meta_timeout":"meta_network_failure";return{...(await failOutboxAttempt(db,input.messageId,reason)),provider:"meta_whatsapp",reason,externalDelivery:false,productionDelivery:false};}clearTimeout(timer);let result:Row={};try{result=await response.json() as Row;}catch{}if(!response.ok){const reason=`meta_http_${response.status}`;if(response.status>=400&&response.status<500&&response.status!==429)return{...(await deadLetterOutbox(db,input.messageId,reason,{httpStatus:response.status})),provider:"meta_whatsapp",externalDelivery:false,productionDelivery:false};return{...(await failOutboxAttempt(db,input.messageId,reason)),provider:"meta_whatsapp",externalDelivery:false,productionDelivery:false};}const messages=Array.isArray(result.messages)?result.messages as Row[]:[],reference=text(messages[0]?.id);if(!reference)return{...(await failOutboxAttempt(db,input.messageId,"meta_message_id_missing")),provider:"meta_whatsapp",externalDelivery:false,productionDelivery:false};await db.prepare("UPDATE communication_messages SET provider='meta_whatsapp',provider_reference=?,updated_at=? WHERE id=?").bind(reference,Date.now(),input.messageId).run();await recordAtomicDeliveryEvent(db,{messageId:input.messageId,provider:"meta_whatsapp",eventId:`accepted:${reference}`,eventType:"accepted",providerReference:reference,detail:{environment:"production",productionDelivery:true}});return{status:"provider_accepted",provider:"meta_whatsapp",providerReference:reference,httpStatus:response.status,externalDelivery:true,productionDelivery:true};}
