export type WalkingScheduleResult={groupId:string;status:string;provider?:{id:string;name:string;model:"full_time"|"commission"};reservation?:{id:string;start:string;end:string};duplicatePrevented:boolean};
export async function reserveWalkingSchedule(input:{groupId:string;customerId:string;petIds:string[];zoneId:string;scheduledStart:string;scheduledEnd:string;preferredProviderId?:string}){const response=await fetch("/api/walking-scheduling",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(input)});const body=await response.json() as {data?:WalkingScheduleResult;error?:string};if(!response.ok||!body.data)throw new Error(body.error??"Walking schedule could not be reserved");return body.data;}
