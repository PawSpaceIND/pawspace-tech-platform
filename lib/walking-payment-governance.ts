type Row=Record<string,unknown>;
export async function ensureWalkingPaymentTables(db:D1Database){await db.prepare("CREATE TABLE IF NOT EXISTS walking_quote_payment_attestations (quote_id TEXT PRIMARY KEY,status TEXT NOT NULL,amount REAL NOT NULL,currency TEXT NOT NULL DEFAULT 'INR',environment TEXT NOT NULL DEFAULT 'sandbox',reference TEXT NOT NULL,created_at INTEGER NOT NULL,updated_at INTEGER NOT NULL)").run();}
export async function captureWalkingQuoteSandbox(db:D1Database,input:{quoteId:string;amount:number}){await ensureWalkingPaymentTables(db);const quote=await db.prepare("SELECT id,total_amount,status,expires_at FROM walking_commercial_quotes WHERE id=?").bind(input.quoteId).first<Row>();if(!quote)throw new Response("Walking quote not found",{status:404});if(String(quote.status)!=="open")throw new Response("Only an open Walking quote can be sandbox-captured",{status:409});if(Number(quote.expires_at)<Date.now())throw new Response("Walking quote expired before sandbox capture",{status:409});if(Number(quote.total_amount)!==Number(input.amount))throw new Response("Sandbox capture amount must match the Walking quote",{status:409});const existing=await db.prepare("SELECT * FROM walking_quote_payment_attestations WHERE quote_id=?").bind(input.quoteId).first<Row>();if(existing&&String(existing.status)==="captured")return{quoteId:input.quoteId,status:"captured" as const,amount:Number(existing.amount),currency:String(existing.currency),environment:"sandbox" as const,reference:String(existing.reference),duplicatePrevented:true};const now=Date.now(),reference=`WALK-UAT-PAY-${crypto.randomUUID().slice(0,12).toUpperCase()}`;await db.prepare("INSERT INTO walking_quote_payment_attestations (quote_id,status,amount,currency,environment,reference,created_at,updated_at) VALUES (?,'captured',?,'INR','sandbox',?,?,?) ON CONFLICT(quote_id) DO UPDATE SET status='captured',amount=excluded.amount,reference=excluded.reference,updated_at=excluded.updated_at").bind(input.quoteId,input.amount,reference,now,now).run();return{quoteId:input.quoteId,status:"captured" as const,amount:input.amount,currency:"INR",environment:"sandbox" as const,reference,duplicatePrevented:false};}
export async function requireWalkingQuoteSandboxCapture(db:D1Database,input:{quoteId:string;amount:number}){await ensureWalkingPaymentTables(db);const row=await db.prepare("SELECT * FROM walking_quote_payment_attestations WHERE quote_id=?").bind(input.quoteId).first<Row>();if(!row||String(row.status)!=="captured")throw new Response("Walking quote requires server-confirmed sandbox capture before booking",{status:409});if(String(row.environment)!=="sandbox")throw new Response("Walking Gate 1 only accepts sandbox payment attestation",{status:409});if(Number(row.amount)!==Number(input.amount))throw new Response("Walking sandbox capture does not match the governed quote amount",{status:409});return{status:"captured" as const,reference:String(row.reference),environment:"sandbox" as const};}
