export type LeadAssignmentPolicyStatus="draft"|"active_uat"|"retired";
export type LeadAssignmentReason="new_lead"|"continuity"|"reassignment"|"reopened"|"manager_override";

type Db=D1Database;
type Row=Record<string,unknown>;
const text=(value:unknown)=>String(value??"").trim();
const list=(value:unknown)=>{if(Array.isArray(value))return value.map(text).filter(Boolean);try{return JSON.parse(String(value||"[]")) as string[]}catch{return[] as string[]}};
const uid=(prefix:string)=>`${prefix}-${crypto.randomUUID().slice(0,12).toUpperCase()}`;
const positiveInt=(value:unknown,name:string)=>{const n=Number(value);if(!Number.isInteger(n)||n<1)throw new Error(`${name} must be a positive integer`);return n;};

export async function ensureLeadAssignmentTables(db:Db){await db.batch([
 db.prepare("CREATE TABLE IF NOT EXISTS lead_assignment_policies (id TEXT PRIMARY KEY,name TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'draft',version INTEGER NOT NULL DEFAULT 1,team_code TEXT NOT NULL,service_codes_json TEXT NOT NULL,city_ids_json TEXT NOT NULL,language_codes_json TEXT NOT NULL,max_active_workload INTEGER NOT NULL,continuity_enabled INTEGER NOT NULL DEFAULT 1,require_shift INTEGER NOT NULL DEFAULT 0,fallback_queue TEXT NOT NULL,effective_from INTEGER NOT NULL,effective_until INTEGER,approval_reference TEXT,created_by TEXT NOT NULL,created_at INTEGER NOT NULL,updated_by TEXT NOT NULL,updated_at INTEGER NOT NULL)"),
 db.prepare("CREATE TABLE IF NOT EXISTS lead_assignment_policy_versions (id TEXT PRIMARY KEY,policy_id TEXT NOT NULL,version INTEGER NOT NULL,snapshot_json TEXT NOT NULL,reason TEXT NOT NULL,actor_id TEXT NOT NULL,created_at INTEGER NOT NULL)"),
 db.prepare("CREATE TABLE IF NOT EXISTS lead_assignment_memberships (id TEXT PRIMARY KEY,employee_email TEXT NOT NULL,team_code TEXT NOT NULL,service_codes_json TEXT NOT NULL,city_ids_json TEXT NOT NULL,language_codes_json TEXT NOT NULL,active INTEGER NOT NULL DEFAULT 1,workload_cap_override INTEGER,created_by TEXT NOT NULL,created_at INTEGER NOT NULL,updated_by TEXT NOT NULL,updated_at INTEGER NOT NULL,UNIQUE(employee_email,team_code))"),
 db.prepare("CREATE TABLE IF NOT EXISTS lead_assignment_availability (id TEXT PRIMARY KEY,employee_email TEXT NOT NULL,available_from INTEGER NOT NULL,available_until INTEGER NOT NULL,status TEXT NOT NULL DEFAULT 'available',reason TEXT,created_by TEXT NOT NULL,created_at INTEGER NOT NULL)"),
 db.prepare("CREATE INDEX IF NOT EXISTS lead_assignment_availability_employee_idx ON lead_assignment_availability(employee_email,available_from,available_until)"),
 db.prepare("CREATE TABLE IF NOT EXISTS lead_assignments (id TEXT PRIMARY KEY,idempotency_key TEXT NOT NULL UNIQUE,lead_id TEXT NOT NULL,employee_email TEXT,team_code TEXT NOT NULL,policy_id TEXT NOT NULL,policy_version INTEGER NOT NULL,assignment_reason TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'current',fallback_queue TEXT,assigned_at INTEGER NOT NULL,accepted_at INTEGER,ended_at INTEGER,ended_reason TEXT,previous_assignment_id TEXT,detail_json TEXT NOT NULL DEFAULT '{}',created_by TEXT NOT NULL,created_at INTEGER NOT NULL)"),
 db.prepare("CREATE UNIQUE INDEX IF NOT EXISTS lead_assignments_one_current_idx ON lead_assignments(lead_id) WHERE status='current'"),
 db.prepare("CREATE TABLE IF NOT EXISTS lead_assignment_events (id TEXT PRIMARY KEY,assignment_id TEXT NOT NULL,lead_id TEXT NOT NULL,event_type TEXT NOT NULL,actor_id TEXT NOT NULL,detail_json TEXT NOT NULL DEFAULT '{}',created_at INTEGER NOT NULL)"),
]);}

function policySnapshot(row:Row){return{id:text(row.id),name:text(row.name),status:text(row.status) as LeadAssignmentPolicyStatus,version:Number(row.version||1),teamCode:text(row.team_code),serviceCodes:list(row.service_codes_json),cityIds:list(row.city_ids_json),languageCodes:list(row.language_codes_json),maxActiveWorkload:Number(row.max_active_workload),continuityEnabled:Number(row.continuity_enabled)===1,requireShift:Number(row.require_shift)===1,fallbackQueue:text(row.fallback_queue),effectiveFrom:Number(row.effective_from),effectiveUntil:row.effective_until==null?null:Number(row.effective_until),approvalReference:row.approval_reference?text(row.approval_reference):null,createdBy:text(row.created_by),createdAt:Number(row.created_at),updatedBy:text(row.updated_by),updatedAt:Number(row.updated_at)};}
function validatePolicy(input:{name:string;teamCode:string;serviceCodes:string[];cityIds:string[];languageCodes:string[];maxActiveWorkload:number;fallbackQueue:string;effectiveFrom:number;effectiveUntil?:number|null}){if(input.name.trim().length<3)throw new Error("Lead assignment policy name is required");if(!input.teamCode.trim())throw new Error("Lead assignment team is required");if(!input.serviceCodes.length)throw new Error("At least one service is required");if(!input.cityIds.length)throw new Error("At least one city is required");positiveInt(input.maxActiveWorkload,"Max active workload");if(!input.fallbackQueue.trim())throw new Error("Fallback queue is required");if(!Number.isFinite(input.effectiveFrom)||input.effectiveFrom<=0)throw new Error("Policy effective-from is required");if(input.effectiveUntil!=null&&input.effectiveUntil<=input.effectiveFrom)throw new Error("Policy effective-until must be after effective-from");}
async function versionPolicy(db:Db,row:Row,reason:string,actorId:string){await db.prepare("INSERT INTO lead_assignment_policy_versions (id,policy_id,version,snapshot_json,reason,actor_id,created_at) VALUES (?,?,?,?,?,?,?)").bind(uid("LAPV"),row.id,row.version,JSON.stringify(policySnapshot(row)),reason,actorId,Date.now()).run();}

export async function saveLeadAssignmentPolicy(db:Db,input:{id?:string;name:string;teamCode:string;serviceCodes:string[];cityIds:string[];languageCodes?:string[];maxActiveWorkload:number;continuityEnabled:boolean;requireShift:boolean;fallbackQueue:string;effectiveFrom:number;effectiveUntil?:number|null;reason:string;actorId:string}){await ensureLeadAssignmentTables(db);const normalized={name:input.name.trim(),teamCode:input.teamCode.trim(),serviceCodes:[...new Set(input.serviceCodes.map(text).filter(Boolean))],cityIds:[...new Set(input.cityIds.map(text).filter(Boolean))],languageCodes:[...new Set((input.languageCodes||[]).map(text).filter(Boolean))],maxActiveWorkload:positiveInt(input.maxActiveWorkload,"Max active workload"),continuityEnabled:Boolean(input.continuityEnabled),requireShift:Boolean(input.requireShift),fallbackQueue:input.fallbackQueue.trim(),effectiveFrom:Number(input.effectiveFrom),effectiveUntil:input.effectiveUntil==null?null:Number(input.effectiveUntil)};validatePolicy(normalized);if(input.reason.trim().length<8)throw new Error("A clear policy change reason is required");const now=Date.now(),policyId=input.id?.trim()||uid("LAP"),prior=await db.prepare("SELECT * FROM lead_assignment_policies WHERE id=?").bind(policyId).first<Row>();if(prior&&text(prior.status)!=="draft")throw new Error("Active/retired assignment policy is immutable; create a new policy version instead");const nextVersion=prior?Number(prior.version||1)+1:1;if(prior){await db.prepare("UPDATE lead_assignment_policies SET name=?,version=?,team_code=?,service_codes_json=?,city_ids_json=?,language_codes_json=?,max_active_workload=?,continuity_enabled=?,require_shift=?,fallback_queue=?,effective_from=?,effective_until=?,updated_by=?,updated_at=? WHERE id=?").bind(normalized.name,nextVersion,normalized.teamCode,JSON.stringify(normalized.serviceCodes),JSON.stringify(normalized.cityIds),JSON.stringify(normalized.languageCodes),normalized.maxActiveWorkload,normalized.continuityEnabled?1:0,normalized.requireShift?1:0,normalized.fallbackQueue,normalized.effectiveFrom,normalized.effectiveUntil,input.actorId,now,policyId).run();}else{await db.prepare("INSERT INTO lead_assignment_policies (id,name,status,version,team_code,service_codes_json,city_ids_json,language_codes_json,max_active_workload,continuity_enabled,require_shift,fallback_queue,effective_from,effective_until,approval_reference,created_by,created_at,updated_by,updated_at) VALUES (?,?,'draft',1,?,?,?,?,?,?,?,?,?,?,NULL,?,?,?,?)").bind(policyId,normalized.name,normalized.teamCode,JSON.stringify(normalized.serviceCodes),JSON.stringify(normalized.cityIds),JSON.stringify(normalized.languageCodes),normalized.maxActiveWorkload,normalized.continuityEnabled?1:0,normalized.requireShift?1:0,normalized.fallbackQueue,normalized.effectiveFrom,normalized.effectiveUntil,input.actorId,now,input.actorId,now).run();}const row=await db.prepare("SELECT * FROM lead_assignment_policies WHERE id=?").bind(policyId).first<Row>();if(!row)throw new Error("Lead assignment policy could not be saved");await versionPolicy(db,row,input.reason,input.actorId);return policySnapshot(row);}

export async function activateLeadAssignmentPolicy(db:Db,input:{policyId:string;approvalReference:string;reason:string;actorId:string}){await ensureLeadAssignmentTables(db);if(input.approvalReference.trim().length<4)throw new Error("Assignment policy activation requires an approval reference");if(input.reason.trim().length<8)throw new Error("A clear activation reason is required");const row=await db.prepare("SELECT * FROM lead_assignment_policies WHERE id=?").bind(input.policyId).first<Row>();if(!row)throw new Error("Lead assignment policy not found");if(text(row.status)!=="draft")throw new Error("Only draft assignment policy can be activated");const snapshot=policySnapshot(row);validatePolicy({name:snapshot.name,teamCode:snapshot.teamCode,serviceCodes:snapshot.serviceCodes,cityIds:snapshot.cityIds,languageCodes:snapshot.languageCodes,maxActiveWorkload:snapshot.maxActiveWorkload,fallbackQueue:snapshot.fallbackQueue,effectiveFrom:snapshot.effectiveFrom,effectiveUntil:snapshot.effectiveUntil});const now=Date.now();await db.prepare("UPDATE lead_assignment_policies SET status='active_uat',approval_reference=?,updated_by=?,updated_at=? WHERE id=?").bind(input.approvalReference.trim(),input.actorId,now,input.policyId).run();const after=await db.prepare("SELECT * FROM lead_assignment_policies WHERE id=?").bind(input.policyId).first<Row>();await versionPolicy(db,after!,input.reason,input.actorId);return policySnapshot(after!);}

export async function saveLeadAssignmentMember(db:Db,input:{employeeEmail:string;teamCode:string;serviceCodes:string[];cityIds:string[];languageCodes?:string[];active:boolean;workloadCapOverride?:number|null;actorId:string}){await ensureLeadAssignmentTables(db);const email=input.employeeEmail.trim().toLowerCase();if(!email||!input.teamCode.trim())throw new Error("Employee email and team are required");const user=await db.prepare("SELECT email,status FROM app_users WHERE email=?").bind(email).first<Row>();if(!user||text(user.status)!=="active")throw new Error("Lead assignment member must be an active platform user");const services=[...new Set(input.serviceCodes.map(text).filter(Boolean))],cities=[...new Set(input.cityIds.map(text).filter(Boolean))],languages=[...new Set((input.languageCodes||[]).map(text).filter(Boolean))];if(!services.length||!cities.length)throw new Error("Member service and city scope are required");const override=input.workloadCapOverride==null?null:positiveInt(input.workloadCapOverride,"Workload cap override"),now=Date.now(),id=uid("LAM");await db.prepare("INSERT INTO lead_assignment_memberships (id,employee_email,team_code,service_codes_json,city_ids_json,language_codes_json,active,workload_cap_override,created_by,created_at,updated_by,updated_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?) ON CONFLICT(employee_email,team_code) DO UPDATE SET service_codes_json=excluded.service_codes_json,city_ids_json=excluded.city_ids_json,language_codes_json=excluded.language_codes_json,active=excluded.active,workload_cap_override=excluded.workload_cap_override,updated_by=excluded.updated_by,updated_at=excluded.updated_at").bind(id,email,input.teamCode.trim(),JSON.stringify(services),JSON.stringify(cities),JSON.stringify(languages),input.active?1:0,override,input.actorId,now,input.actorId,now).run();return{employeeEmail:email,teamCode:input.teamCode.trim(),serviceCodes:services,cityIds:cities,languageCodes:languages,active:input.active,workloadCapOverride:override};}

export async function setLeadAssignmentAvailability(db:Db,input:{employeeEmail:string;availableFrom:number;availableUntil:number;status?:"available"|"unavailable";reason?:string;actorId:string}){await ensureLeadAssignmentTables(db);const email=input.employeeEmail.trim().toLowerCase();if(!email||!Number.isFinite(input.availableFrom)||!Number.isFinite(input.availableUntil)||input.availableUntil<=input.availableFrom)throw new Error("Valid employee availability window is required");const now=Date.now(),id=uid("LAV");await db.prepare("INSERT INTO lead_assignment_availability (id,employee_email,available_from,available_until,status,reason,created_by,created_at) VALUES (?,?,?,?,?,?,?,?)").bind(id,email,input.availableFrom,input.availableUntil,input.status||"available",input.reason?.trim()||null,input.actorId,now).run();return{id,employeeEmail:email,availableFrom:input.availableFrom,availableUntil:input.availableUntil,status:input.status||"available"};}

async function leadContext(db:Db,leadId:string){const lead=await db.prepare("SELECT l.id,l.customer_id,l.service,l.source,l.status,l.owner,c.area,c.primary_phone,c.email FROM lead_work_items l LEFT JOIN crm_contacts c ON c.id=l.customer_id WHERE l.id=?").bind(leadId).first<Row>();if(!lead)throw new Error("Lead not found");return{id:text(lead.id),customerId:text(lead.customer_id),service:text(lead.service).toLowerCase().replaceAll(" ","_"),city:text(lead.area)||"Bengaluru",source:text(lead.source),status:text(lead.status),legacyOwner:text(lead.owner),phone:text(lead.primary_phone),email:text(lead.email)};}
function cityMatches(memberCities:string[],leadCity:string,policyCities:string[]){const normalized=leadCity.toLowerCase();return memberCities.some(city=>normalized.includes(city.toLowerCase()))&&policyCities.some(city=>normalized.includes(city.toLowerCase()));}
async function activePolicy(db:Db,lead:{service:string;city:string},asOf:number){const rows=await db.prepare("SELECT * FROM lead_assignment_policies WHERE status='active_uat' AND effective_from<=? AND (effective_until IS NULL OR effective_until>=?) ORDER BY updated_at DESC").bind(asOf,asOf).all<Row>();return rows.results.find(row=>list(row.service_codes_json).includes(lead.service)&&list(row.city_ids_json).some(city=>lead.city.toLowerCase().includes(city.toLowerCase())))||null;}
async function candidateRows(db:Db,policy:Row,lead:{service:string;city:string},asOf:number,excludeEmployeeEmail?:string|null){const memberships=await db.prepare("SELECT m.*,u.status user_status,u.name user_name FROM lead_assignment_memberships m JOIN app_users u ON u.email=m.employee_email WHERE m.team_code=? AND m.active=1 AND u.status='active' ORDER BY m.employee_email").bind(policy.team_code).all<Row>();const result:Array<Row&{activeLoad:number;lastAssignedAt:number;effectiveCap:number;available:boolean}>=[];for(const member of memberships.results){if(excludeEmployeeEmail&&text(member.employee_email).toLowerCase()===excludeEmployeeEmail.toLowerCase())continue;const services=list(member.service_codes_json),cities=list(member.city_ids_json);if(!services.includes(lead.service)||!cityMatches(cities,lead.city,list(policy.city_ids_json)))continue;let available=true;if(Number(policy.require_shift)===1){const availability=await db.prepare("SELECT status FROM lead_assignment_availability WHERE employee_email=? AND available_from<=? AND available_until>=? ORDER BY created_at DESC LIMIT 1").bind(member.employee_email,asOf,asOf).first<Row>();available=text(availability?.status)==="available";}if(!available)continue;const load=await db.prepare("SELECT COUNT(*) count,COALESCE(MAX(assigned_at),0) last_assigned FROM lead_assignments WHERE employee_email=? AND status='current'").bind(member.employee_email).first<Row>();const effectiveCap=member.workload_cap_override==null?Number(policy.max_active_workload):Number(member.workload_cap_override);result.push({...member,activeLoad:Number(load?.count||0),lastAssignedAt:Number(load?.last_assigned||0),effectiveCap,available});}return result.filter(item=>item.activeLoad<item.effectiveCap).sort((a,b)=>a.activeLoad-b.activeLoad||a.lastAssignedAt-b.lastAssignedAt||text(a.employee_email).localeCompare(text(b.employee_email)));}
async function currentAssignment(db:Db,leadId:string){return db.prepare("SELECT * FROM lead_assignments WHERE lead_id=? AND status='current'").bind(leadId).first<Row>();}
async function assignmentEvent(db:Db,assignmentId:string,leadId:string,eventType:string,actorId:string,detail:unknown){await db.prepare("INSERT INTO lead_assignment_events (id,assignment_id,lead_id,event_type,actor_id,detail_json,created_at) VALUES (?,?,?,?,?,?,?)").bind(uid("LAE"),assignmentId,leadId,eventType,actorId,JSON.stringify(detail||{}),Date.now()).run();}

export async function assignLead(db:Db,input:{leadId:string;idempotencyKey:string;reason:LeadAssignmentReason;actorId:string;preferredEmployeeEmail?:string|null;excludeEmployeeEmail?:string|null;asOf?:number}){await ensureLeadAssignmentTables(db);if(!input.leadId.trim()||!input.idempotencyKey.trim())throw new Error("Lead and idempotency key are required");const prior=await db.prepare("SELECT * FROM lead_assignments WHERE idempotency_key=?").bind(input.idempotencyKey).first<Row>();if(prior)return{assignment:prior,duplicatePrevented:true};const lead=await leadContext(db,input.leadId),now=input.asOf??Date.now(),policy=await activePolicy(db,lead,now);if(!policy)throw new Error("No active lead assignment policy matches this lead service/city");const existing=await currentAssignment(db,input.leadId);if(existing)return{assignment:existing,duplicatePrevented:true,alreadyAssigned:true};const candidates=await candidateRows(db,policy,lead,now,input.excludeEmployeeEmail);let selected:typeof candidates[number]|undefined,selectionReason=input.reason;
 if(Number(policy.continuity_enabled)===1&&lead.legacyOwner&&lead.legacyOwner!=="Unassigned"){selected=candidates.find(candidate=>text(candidate.employee_email).toLowerCase()===lead.legacyOwner.toLowerCase()||text(candidate.user_name).toLowerCase()===lead.legacyOwner.toLowerCase());if(selected)selectionReason="continuity";}
 if(input.preferredEmployeeEmail){const preferred=candidates.find(candidate=>text(candidate.employee_email).toLowerCase()===input.preferredEmployeeEmail!.trim().toLowerCase());if(!preferred)throw new Error("Preferred lead owner is not currently eligible");selected=preferred;selectionReason=input.reason==="manager_override"?"manager_override":input.reason;}
 selected=selected||candidates[0];const assignmentId=uid("LAS"),fallbackQueue=text(policy.fallback_queue),employeeEmail=selected?text(selected.employee_email):null,detail={selection:employeeEmail?"eligible_lowest_workload":"fallback_queue",candidateCount:candidates.length,activeLoad:selected?.activeLoad??null,effectiveCap:selected?.effectiveCap??null,legacyOwnerProjection:lead.legacyOwner||null,canonicalOwnerAuthority:"lead_assignments"};await db.prepare("INSERT INTO lead_assignments (id,idempotency_key,lead_id,employee_email,team_code,policy_id,policy_version,assignment_reason,status,fallback_queue,assigned_at,detail_json,created_by,created_at) VALUES (?,?,?,?,?,?,?,?, 'current',?,?,?,?,?)").bind(assignmentId,input.idempotencyKey,input.leadId,employeeEmail,policy.team_code,policy.id,policy.version,selectionReason,employeeEmail?null:fallbackQueue,now,JSON.stringify(detail),input.actorId,now).run();if(employeeEmail)await db.prepare("UPDATE lead_work_items SET owner=?,assigned_at=?,updated_at=? WHERE id=?").bind(employeeEmail,now,now,input.leadId).run();else await db.prepare("UPDATE lead_work_items SET owner='Unassigned',assigned_at=?,updated_at=? WHERE id=?").bind(now,now,input.leadId).run();await assignmentEvent(db,assignmentId,input.leadId,employeeEmail?"assigned":"fallback_queued",input.actorId,{employeeEmail,fallbackQueue,policyId:policy.id,policyVersion:policy.version,selectionReason,...detail});const assignment=await db.prepare("SELECT * FROM lead_assignments WHERE id=?").bind(assignmentId).first<Row>();return{assignment,duplicatePrevented:false,fallback:!employeeEmail};}

export async function reassignLead(db:Db,input:{leadId:string;idempotencyKey:string;reason:string;actorId:string;preferredEmployeeEmail?:string|null;excludeEmployeeEmail?:string|null;asOf?:number}){await ensureLeadAssignmentTables(db);if(input.reason.trim().length<8)throw new Error("Reassignment requires a clear reason");const prior=await db.prepare("SELECT * FROM lead_assignments WHERE idempotency_key=?").bind(input.idempotencyKey).first<Row>();if(prior)return{assignment:prior,duplicatePrevented:true};const current=await currentAssignment(db,input.leadId);if(!current)throw new Error("Lead has no current assignment to replace");const now=input.asOf??Date.now();await db.prepare("UPDATE lead_assignments SET status='superseded',ended_at=?,ended_reason=? WHERE id=?").bind(now,input.reason.trim(),current.id).run();await assignmentEvent(db,text(current.id),input.leadId,"superseded",input.actorId,{reason:input.reason});let result;try{result=await assignLead(db,{leadId:input.leadId,idempotencyKey:input.idempotencyKey,reason:input.preferredEmployeeEmail?"manager_override":"reassignment",actorId:input.actorId,preferredEmployeeEmail:input.preferredEmployeeEmail,excludeEmployeeEmail:input.preferredEmployeeEmail?null:(input.excludeEmployeeEmail??text(current.employee_email)),asOf:now});}catch(error){await db.prepare("UPDATE lead_assignments SET status='current',ended_at=NULL,ended_reason=NULL WHERE id=?").bind(current.id).run();await assignmentEvent(db,text(current.id),input.leadId,"supersede_reverted",input.actorId,{reason:"New assignment failed, original assignment restored",error:error instanceof Error?error.message:String(error)});throw error;}const newId=text((result.assignment as Row).id);await db.prepare("UPDATE lead_assignments SET previous_assignment_id=?,detail_json=json_set(detail_json,'$.reassignmentReason',?) WHERE id=?").bind(current.id,input.reason.trim(),newId).run();await assignmentEvent(db,newId,input.leadId,"reassigned",input.actorId,{previousAssignmentId:current.id,reason:input.reason});return result;}

export async function acceptLeadAssignment(db:Db,input:{assignmentId:string;actorEmail:string}){await ensureLeadAssignmentTables(db);const assignment=await db.prepare("SELECT * FROM lead_assignments WHERE id=?").bind(input.assignmentId).first<Row>();if(!assignment)throw new Error("Lead assignment not found");if(text(assignment.status)!=="current")throw new Error("Only current assignment can be acknowledged");if(text(assignment.employee_email).toLowerCase()!==input.actorEmail.trim().toLowerCase())throw new Error("Only the assigned employee can acknowledge this lead");if(assignment.accepted_at)return{assignmentId:input.assignmentId,acceptedAt:Number(assignment.accepted_at),duplicatePrevented:true};const now=Date.now();await db.prepare("UPDATE lead_assignments SET accepted_at=? WHERE id=?").bind(now,input.assignmentId).run();await assignmentEvent(db,input.assignmentId,text(assignment.lead_id),"accepted",input.actorEmail,{});return{assignmentId:input.assignmentId,acceptedAt:now,duplicatePrevented:false};}

export async function endLeadAssignment(db:Db,input:{leadId:string;reason:string;actorId:string}){await ensureLeadAssignmentTables(db);if(input.reason.trim().length<4)throw new Error("Assignment end reason is required");const current=await currentAssignment(db,input.leadId);if(!current)return{leadId:input.leadId,ended:false};const now=Date.now();await db.prepare("UPDATE lead_assignments SET status='ended',ended_at=?,ended_reason=? WHERE id=?").bind(now,input.reason.trim(),current.id).run();await assignmentEvent(db,text(current.id),input.leadId,"ended",input.actorId,{reason:input.reason});return{leadId:input.leadId,assignmentId:text(current.id),ended:true};}

export async function leadAssignmentDirectory(db:Db){await ensureLeadAssignmentTables(db);const[policies,members,current,events]=await Promise.all([db.prepare("SELECT * FROM lead_assignment_policies ORDER BY updated_at DESC").all<Row>(),db.prepare("SELECT m.*,u.name user_name,u.status user_status FROM lead_assignment_memberships m LEFT JOIN app_users u ON u.email=m.employee_email ORDER BY m.team_code,m.employee_email").all<Row>(),db.prepare("SELECT a.*,l.customer_id,l.service,l.source,l.owner legacy_owner FROM lead_assignments a LEFT JOIN lead_work_items l ON l.id=a.lead_id WHERE a.status='current' ORDER BY a.assigned_at DESC").all<Row>(),db.prepare("SELECT * FROM lead_assignment_events ORDER BY created_at DESC LIMIT 200").all<Row>()]);return{policies:policies.results.map(policySnapshot),members:members.results,currentAssignments:current.results.map(row=>({...row,projectionMismatch:Boolean(row.employee_email)&&text(row.employee_email).toLowerCase()!==text(row.legacy_owner).toLowerCase()})),events:events.results,truth:{canonicalOwnerSource:"lead_assignments",legacyLeadOwnerField:"projection_only",hardCodedOwnerRotationAuthoritative:false,productionReady:false}};}

export async function leadAssignmentIntegrity(db:Db){await ensureLeadAssignmentTables(db);const rows=await db.prepare("SELECT a.lead_id,a.employee_email canonical_owner,l.owner legacy_owner,a.policy_id,a.policy_version,a.assigned_at FROM lead_assignments a JOIN lead_work_items l ON l.id=a.lead_id WHERE a.status='current' AND COALESCE(a.employee_email,'Unassigned')!=COALESCE(l.owner,'Unassigned') ORDER BY a.assigned_at DESC").all<Row>();return{projectionMismatches:rows.results,count:rows.results.length,canonicalOwnerSource:"lead_assignments"};}
