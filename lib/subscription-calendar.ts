export function addCalendarMonthsClamped(startedAt:number,months:number){if(!Number.isFinite(startedAt)||!Number.isInteger(months)||months<0)throw new Error("invalid_calendar_month_addition");const original=new Date(startedAt),day=original.getUTCDate(),target=new Date(startedAt);target.setUTCDate(1);target.setUTCMonth(target.getUTCMonth()+months);const lastDay=new Date(Date.UTC(target.getUTCFullYear(),target.getUTCMonth()+1,0)).getUTCDate();target.setUTCDate(Math.min(day,lastDay));return target.getTime();}
