export type WalkingPackage={package_code:string;name:string;duration_minutes:30|60;base_price:number;extra_pet_price:number;currency:string;max_pets:number;version:number};
export type WalkingQuote={quoteId:string;packageCode:string;packageName:string;packageVersion:number;durationMinutes:30|60;petCount:number;scheduledStart:string;scheduledEnd:string;totalAmount:number;amountDueNow:number;paymentMode:"prepaid";expiresAt:number;liveMoney:false};

export async function loadWalkingCatalogue(input:{scheduledStart?:string}={}){const query=new URLSearchParams();if(input.scheduledStart)query.set("scheduledStart",input.scheduledStart);const response=await fetch(`/api/walking-commercial${query.size?`?${query}`:""}`,{cache:"no-store"});const body=await response.json();if(!response.ok)throw new Error(body.error||"Unable to load Walking catalogue");return body.data as {packages:WalkingPackage[];source:string;availabilityMode:string;availabilityVerified:boolean;liveAvailability:boolean;liveMoney:boolean};}
export async function createWalkingQuote(input:{packageCode:string;petCount:number;scheduledStart:string;scheduledEnd:string;paymentMode?:"prepaid";recurring?:boolean;couponCode?:string}){const response=await fetch("/api/walking-commercial",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...input,paymentMode:input.paymentMode||"prepaid"})});const body=await response.json();if(!response.ok)throw new Error(body.error||"Unable to create Walking quote");return body.data as WalkingQuote;}
