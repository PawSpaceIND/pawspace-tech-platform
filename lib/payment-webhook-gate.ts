import{parsePaymentEnvironment,type PaymentEnvironment}from"./payment-environment";
type Env=Record<string,unknown>;
const val=(env:Env,key:string)=>String(env?.[key]??"").trim();
const isTrue=(v:unknown)=>String(v??"").trim().toLowerCase()==="true";
export type WebhookGate=|{ok:true;environment:PaymentEnvironment;secret:string}|{ok:false;status:number;reason:string};
export function paymentMode(env:Env):PaymentEnvironment{return parsePaymentEnvironment(env);}
export function resolvePaymentWebhookGate(env:Env):WebhookGate{let mode:PaymentEnvironment;try{mode=paymentMode(env);}catch(error){return{ok:false,status:503,reason:error instanceof Error?error.message:String(error)};}if(mode==="sandbox"){const secret=val(env,"RAZORPAY_WEBHOOK_SECRET_SANDBOX");if(!secret)return{ok:false,status:503,reason:"Razorpay sandbox webhook secret is not configured (RAZORPAY_WEBHOOK_SECRET_SANDBOX)"};return{ok:true,environment:"sandbox",secret};}if(!isTrue(env?.PAWSPACE_PAYMENT_LIVE_APPROVED))return{ok:false,status:503,reason:'Live payments are not approved (set PAWSPACE_PAYMENT_LIVE_APPROVED="true"). Unlock and verify in isolated staging first.'};const secret=val(env,"RAZORPAY_WEBHOOK_SECRET_LIVE");if(!secret)return{ok:false,status:503,reason:"Razorpay LIVE webhook secret is not configured (RAZORPAY_WEBHOOK_SECRET_LIVE)"};return{ok:true,environment:"live",secret};}
export function paymentWebhookReadiness(env:Env){let mode:PaymentEnvironment|null=null;try{mode=paymentMode(env);}catch{}const gate=resolvePaymentWebhookGate(env);return{environment:mode,sandboxSecretConfigured:Boolean(val(env,"RAZORPAY_WEBHOOK_SECRET_SANDBOX")),liveApproved:isTrue(env?.PAWSPACE_PAYMENT_LIVE_APPROVED),liveSecretConfigured:Boolean(val(env,"RAZORPAY_WEBHOOK_SECRET_LIVE")),receiverReady:gate.ok,activeEnvironment:gate.ok?gate.environment:null,blockedReason:gate.ok?null:gate.reason};}
