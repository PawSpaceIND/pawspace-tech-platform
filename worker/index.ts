/** Cloudflare Worker entry point for the vinext-starter template. */
import{handleImageOptimization,DEFAULT_DEVICE_SIZES,DEFAULT_IMAGE_SIZES}from"vinext/server/image-optimization";import handler from"vinext/server/app-router-entry";import{auditApiResponse,authorizeApiRequest}from"../lib/api-gateway";import{authorizePlatformSessionRequest}from"../lib/session-api-gateway";import{blockDisabledServiceRequest}from"../lib/service-control";import{runBackgroundScheduler}from"../lib/background-scheduler";import{runServiceRecoveryAudioBotSweep}from"../lib/service-recovery-audio-bot";import{processDueWhatsAppNoResponseSequences}from"../lib/whatsapp-no-response-sequence";import{drainCommunicationOutbox}from"../lib/communication-outbox-dispatcher";import{cleanupExpiredReservationLeases}from"../lib/scheduling-reservation-leases";import{runRazorpaySettlementReconciliationSweep}from"../lib/razorpay-settlement-reconciliation";import{runSubscriptionBillingSweep}from"../lib/subscription-billing";import{runSubscriptionScheduledMaintenance}from"../lib/subscription-scheduled";
interface Env{ASSETS:Fetcher;DB:D1Database;FOUNDER_EMAIL?:string;AI?:unknown;IMAGES:{input(stream:ReadableStream):{transform(options:Record<string,unknown>):{output(options:{format:string;quality:number}):Promise<{response():Response}>}}}}interface ExecutionContext{waitUntil(promise:Promise<unknown>):void;passThroughOnException():void}interface ScheduledControllerLike{scheduledTime:number;cron:string;noRetry():void}function secureApiResponse(response:Response){const secured=new Response(response.body,response);secured.headers.set("cache-control","no-store");secured.headers.set("x-content-type-options","nosniff");secured.headers.set("referrer-policy","same-origin");return secured}
const worker={async fetch(request:Request,env:Env,ctx:ExecutionContext):Promise<Response>{const url=new URL(request.url);if(url.pathname.startsWith("/api/")){if(url.pathname==="/api/identity-session")return secureApiResponse(await handler.fetch(request,env,ctx));if(request.method==="POST"&&(url.pathname==="/api/uat-scheduling"||url.pathname==="/api/canonical-bookings"))await cleanupExpiredReservationLeases(env.DB);const sessionAccess=await authorizePlatformSessionRequest(request,env.DB);if(sessionAccess instanceof Response)return sessionAccess;const access=sessionAccess??await authorizeApiRequest(request,env);if(access instanceof Response)return access;const serviceBlock=await blockDisabledServiceRequest(request,env.DB);if(serviceBlock){ctx.waitUntil(auditApiResponse(env,access.actor,access.permission,request,serviceBlock.clone()));return secureApiResponse(serviceBlock)}const response=await handler.fetch(request,env,ctx);ctx.waitUntil(auditApiResponse(env,access.actor,access.permission,request,response.clone()));return secureApiResponse(response)}if(url.pathname==="/_vinext/image"){const allowedWidths=[...DEFAULT_DEVICE_SIZES,...DEFAULT_IMAGE_SIZES];return handleImageOptimization(request,{fetchAsset:path=>env.ASSETS.fetch(new Request(new URL(path,request.url))),transformImage:async(body,{width,format,quality})=>(await env.IMAGES.input(body).transform(width>0?{width}:{}).output({format,quality})).response()},allowedWidths)}return handler.fetch(request,env,ctx)},async scheduled(controller:ScheduledControllerLike,env:Env,ctx:ExecutionContext){ctx.waitUntil((async()=>{const tasks=await Promise.allSettled([cleanupExpiredReservationLeases(env.DB,controller.scheduledTime),runBackgroundScheduler(env.DB,{actorId:"system:scheduled-worker",asOf:controller.scheduledTime,cron:controller.cron}),drainCommunicationOutbox(env.DB,env as unknown as Record<string,unknown>,{asOf:controller.scheduledTime,limit:50}),runServiceRecoveryAudioBotSweep(env.DB,{actorId:"system:scheduled-worker",asOf:controller.scheduledTime,env:env as unknown as Record<string,unknown>}),processDueWhatsAppNoResponseSequences(env.DB,{now:controller.scheduledTime,actorEmail:"system:scheduled-worker"}),runRazorpaySettlementReconciliationSweep(env.DB,env as unknown as Record<string,unknown>,{asOf:controller.scheduledTime}),runSubscriptionScheduledMaintenance(env.DB,env as unknown as Record<string,unknown>,{asOf:controller.scheduledTime,billingSweep:(db,input)=>runSubscriptionBillingSweep(db,input)})]);const names=["reservation cleanup","background scheduler","communication outbox","service recovery audio bot","whatsapp recovery","razorpay settlement reconciliation","subscription maintenance"],errors:string[]=[];tasks.forEach((task,index)=>{if(task.status==="rejected")errors.push(`${names[index]}: ${task.reason instanceof Error?task.reason.message:String(task.reason)}`)});const scheduler=tasks[1],outbox=tasks[2],subscriptions=tasks[6];if(scheduler.status==="fulfilled"&&Array.isArray((scheduler.value as Record<string,unknown>).errors)&&((scheduler.value as Record<string,unknown>).errors as unknown[]).length)errors.push(...(((scheduler.value as Record<string,unknown>).errors as unknown[]).map(value=>String(value))));if(outbox.status==="fulfilled"&&Number((outbox.value as Record<string,unknown>).errors||0)>0)errors.push(`communication outbox: ${(outbox.value as Record<string,unknown>).errors} dispatch exception(s)`);if(subscriptions.status==="fulfilled"&&Number((subscriptions.value as Record<string,unknown>).errors||0)>0)errors.push(`subscription maintenance: ${(subscriptions.value as Record<string,unknown>).errors} exception(s)`);if(errors.length)throw new Error(`Background scheduler partial failure: ${errors.join(" | ")}`)})())}};export default worker;
