import fs from "node:fs";

function replaceOnce(source,search,replacement,label){const i=source.indexOf(search);if(i<0)throw new Error(`Missing ${label}`);if(source.indexOf(search,i+search.length)>=0)throw new Error(`Duplicate ${label}`);return source.slice(0,i)+replacement+source.slice(i+search.length);}
function replaceRegexOnce(source,pattern,replacement,label){const p=new RegExp(pattern.source,pattern.flags.includes("g")?pattern.flags:`${pattern.flags}g`),matches=[...source.matchAll(p)];if(matches.length!==1)throw new Error(`Expected one ${label}, found ${matches.length}`);return source.replace(pattern,replacement);}

const trainingPath="app/mobile-app/training-flow.tsx";
let training=fs.readFileSync(trainingPath,"utf8");
training=replaceOnce(training,'const weekdayMap:Record<string,number[]>={"Tue & Sat":[2,6],"Wed & Sun":[3,0],"Every Saturday":[6],"Choose each session myself":[0,1,2,3,4,5,6]};','const weekdayMap:Record<string,number[]>={"Tue & Sat":[2,6],"Wed & Sun":[3,0],"Every Saturday":[6]};',"schedule map");
training=replaceOnce(training,'days=weekdayMap[frequency]||weekdayMap["Choose each session myself"]','days=weekdayMap[frequency]||weekdayMap["Tue & Sat"]',"start fallback");
training=replaceOnce(training,'weekdayMap[frequency]||weekdayMap["Choose each session myself"]','weekdayMap[frequency]||weekdayMap["Tue & Sat"]',"preview fallback");
training=replaceOnce(training,'    [meet, setMeet] = useState(true),\n','',"bundled Meet state");
training=replaceOnce(training,'  const meetFee=meet&&!meetBookingId?Number(meetPackage?.base_price||0):0;\n','',"Meet fee");
training=replaceOnce(training,'  const payableNow=(checkoutQuote?.amountDueNow??0)+meetFee;','  const payableNow=checkoutQuote?.amountDueNow??0;',"payable amount");
training=replaceOnce(training,'setMeetBookingId(canonical.bookingId);setMeetPetKey(petKey);setMeetTrainerName(decision.provider.name);setMeet(false);setCheckoutQuote(null);','setMeetBookingId(canonical.bookingId);setMeetPetKey(petKey);setMeetTrainerName(decision.provider.name);setCheckoutQuote(null);',"standalone Meet completion");
training=replaceRegexOnce(training,/\n        if\(meet&&!linkedMeetBookingId\)\{.*?setMeetTrainerName\(meetDecision\.provider\.name\);\}/s,'\n        // Meet & Greet is standalone. Package checkout may link an existing meeting but never creates one implicitly.',"bundled Meet creation");
training=replaceOnce(training,'occurrences:quote.sessions,weekdays:weekdayMap[frequency],cadenceDays:frequency==="Choose each session myself"?7:undefined,preferredProviderId:selectedTrainer?.id','occurrences:quote.sessions,weekdays:weekdayMap[frequency],preferredProviderId:selectedTrainer?.id',"fake custom cadence");
training=replaceRegexOnce(training,/\n          <section className=\{styles\.meetTrainer\}>\n            <div className=\{styles\.meetPitch\}>\n              <span>NOT READY TO BUY A PACKAGE\?<\/span>.*?\n          <\/section>/s,'',"post-package Meet block");
training=replaceOnce(training,'              <option>Choose each session myself</option>\n','',"fake custom option");
training=replaceOnce(training,'              <b>{meet&&!meetBookingId ? `${slotLabel(new Date(meetSlot))} · ${meetPackage?Number(meetPackage.direct_minutes_per_pet)+Number(meetPackage.coaching_minutes_per_pet):"—"} min · ${money(meetFee)}` : meetBookingId?`Booked separately · ${meetBookingId}`:"Skipped"}</b>','              <b>{meetBookingId?`Booked separately · ${meetBookingId}`:"Not booked"}</b>',"Meet review");
training=replaceOnce(training,'{money(Math.round(plan.price*plan.splitDuePercent/100)+meetFee)} now · {money(plan.price-Math.round(plan.price*plan.splitDuePercent/100))} later under the canonical split schedule','{money(Math.round(plan.price*plan.splitDuePercent/100))} now · {money(plan.price-Math.round(plan.price*plan.splitDuePercent/100))} later under the canonical split schedule',"split amount");
training=replaceOnce(training,'<span>{money(plan.price + meetFee)} before an eligible coupon</span>','<span>{money(plan.price)} before an eligible coupon</span>',"full amount");
training=replaceOnce(training,'            Book every session now or keep dates flexible. App reminders go 24\n            hours and 2 hours before each session; expiry alerts start 15 days\n            before validity ends.','            Choose one of the supported repeat schedules above. Every package session is shown before payment, and app reminders go 24\n            hours and 2 hours before each session; expiry alerts start 15 days before validity ends.',"calendar guidance");
const meetAlternative=`          <section className={styles.meetTrainer}>\n            <div className={styles.meetPitch}>\n              <span>MEET A TRAINER FIRST</span>\n              <h4>Prefer to meet a trainer before choosing a programme?</h4>\n              <p>Book a separate Meet &amp; Greet now. You can return later and choose a training package without mixing the two purchases.</p>\n            </div>\n            <b>{meetPackage?`${Number(meetPackage.direct_minutes_per_pet)+Number(meetPackage.coaching_minutes_per_pet)}-minute Meet & Greet · ${money(Number(meetPackage.base_price))}`:"Loading Meet & Greet…"}</b>\n            <div className={styles.meetSlots}>\n              {[futureIst(1,11),futureIst(2,15),futureIst(2,16)].map((date)=>{const slot=date.toISOString();return <button key={slot} className={meetSlot===slot?styles.selected:""} onClick={()=>setMeetSlot(slot)}>{slotLabel(date)}<small>{meetSlot===slot?"Selected":"Available"}</small></button>;})}\n            </div>\n            <p>Trainer availability is checked before booking. This creates one standalone, sandbox-paid canonical Meet &amp; Greet only.</p>\n            <button className={styles.meetOnly} onClick={confirmMeetFirst} disabled={scheduling || selectedPets.length === 0 || !selectedTrainer}>{scheduling?"Reserving Meet & Greet…":"Book Meet & Greet only"}</button>\n            {meetLinked&&<article className={styles.meetConfirmed}><b>✓ Meet &amp; Greet booked</b><span>{slotLabel(new Date(meetSlot))} · {meetTrainerName||selectedTrainer?.name||"Assigned trainer"} · {meetBookingId}</span><small>You can continue to a programme now or return after the meeting.</small></article>}\n            {meetBookingId&&!meetLinked&&<article className={styles.meetConfirmed}><b>Meet &amp; Greet belongs to another dog selection</b><span>Select the original dogs to link that meeting, or book another Meet &amp; Greet for the current selection.</span></article>}\n            {scheduleError&&<p role="alert">{scheduleError}</p>}\n          </section>\n`;
training=replaceOnce(training,'          <div className={styles.planGuide}>\n',meetAlternative+'          <div className={styles.planGuide}>\n',"pre-package Meet insertion");
if(training.includes("Choose each session myself"))throw new Error("Fake custom scheduling remains");
if(training.includes("NOT READY TO BUY A PACKAGE?"))throw new Error("Old Meet upsell remains");
fs.writeFileSync(trainingPath,training);

const gatewayPath="lib/api-gateway.ts";
let gateway=fs.readFileSync(gatewayPath,"utf8");
gateway=replaceOnce(gateway,'||url.pathname==="/api/staging-login"','||url.pathname==="/api/staging-login"||url.pathname==="/api/uat-provider-switch"',"UAT switch gateway exemption");
fs.writeFileSync(gatewayPath,gateway);

const testPath="tests/uat-training-partner-closure.test.mjs";
let tests=fs.readFileSync(testPath,"utf8");
if(!tests.includes("UAT provider switch route is production-dead"))tests+=`\nconst providerSwitch=fs.readFileSync(new URL("../app/api/uat-provider-switch/route.ts",import.meta.url),"utf8");\nconst gateway=fs.readFileSync(new URL("../lib/api-gateway.ts",import.meta.url),"utf8");\ntest("UAT provider switch route is production-dead and access-code governed",()=>{assert.match(providerSwitch,/uatLoginEnabled/);assert.match(providerSwitch,/uatAccessCodeValid/);assert.match(providerSwitch,/issuePlatformSession/);assert.match(gateway,/\\/api\\/uat-provider-switch/);});\n`;
fs.writeFileSync(testPath,tests);
console.log("Applied remaining Training UAT closure patch");
