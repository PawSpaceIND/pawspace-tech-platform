import fs from "node:fs";
import { chromium } from "playwright";

const readArg=(name,fallback="")=>{const item=process.argv.find(value=>value.startsWith(`--${name}=`));return item?item.slice(name.length+3):fallback;};
const BASE=readArg("base",process.env.PREVIEW_URL||"").replace(/\/$/,"");
const OUT=readArg("json","customer-ui-acceptance-report.json");
const PHONE=readArg("phone","9000000911"),PIN="560038",FOOD_PIN="560038",PET="UI Test Bruno",CUSTOMER="UI Acceptance Customer",ADDRESS="12 Acceptance Road, Indiranagar, Bengaluru";
const TIMEOUT=Number(readArg("timeout","18000"));
const ASYNC_TIMEOUT=Math.max(TIMEOUT,45000);
const RELEASE_PREVIEW=(()=>{try{return new URL(BASE).hostname.startsWith("pawspace-release-preview.");}catch{return false;}})();
if(!BASE)throw new Error("--base or PREVIEW_URL is required");

const report={generatedAt:new Date().toISOString(),base:BASE,pincode:PIN,cases:[],failures:[],sittingProfileRateEvidence:[]};
const persist=()=>{report.summary={total:report.cases.length,passed:report.cases.filter(x=>x.ok).length,failed:report.cases.filter(x=>!x.ok).length};fs.writeFileSync(OUT,`${JSON.stringify(report,null,2)}\n`);};
const die=(message)=>{throw new Error(message);};
const wait=async(page,ms=350)=>{await page.waitForLoadState("domcontentloaded",{timeout:TIMEOUT}).catch(()=>undefined);await page.waitForTimeout(ms);};
async function actionable(locator,label,timeout=TIMEOUT){await locator.waitFor({state:"visible",timeout}).catch(()=>die(`${label} not visible`));await locator.click({trial:true,timeout}).catch(()=>die(`${label} remained disabled or non-actionable`));}
async function runCase(name,fn){const started=Date.now();try{const detail=await fn();report.cases.push({name,ok:true,ms:Date.now()-started,detail:detail||"passed"});console.log(`PASS ${name}${detail?` — ${detail}`:""}`);}catch(error){const detail=error instanceof Error?error.message:String(error);report.cases.push({name,ok:false,ms:Date.now()-started,detail});report.failures.push({name,detail});console.error(`FAIL ${name} — ${detail}`);}finally{persist();}}
async function gotoApp(page){const response=await page.goto(`${BASE}/mobile-app`,{waitUntil:"domcontentloaded",timeout:TIMEOUT});if(!response||response.status()>=500)die(`mobile app HTTP ${response?.status()??0}`);await wait(page);}
async function nav(page,label){const target=page.locator("nav").getByRole("button").filter({hasText:new RegExp(label,"i")}).last();await target.waitFor({state:"visible",timeout:TIMEOUT}).catch(()=>die(`bottom navigation ${label} not found`));await target.click();await wait(page,220);}
async function text(page,value){await page.getByText(value,{exact:false}).first().waitFor({state:"visible",timeout:TIMEOUT});}
const apiPath=(response,path,method="GET")=>{try{return new URL(response.url()).pathname===path&&response.request().method()===method;}catch{return false;}};
async function responseDetail(response){if(!response)return"no response";const body=await response.json().catch(()=>({}));return String(body?.error||body?.message||`HTTP ${response.status()}`);}

async function login(page,context){
 await gotoApp(page);const session=await context.request.get(`${BASE}/api/identity-session`);if(session.ok()){const body=await session.json().catch(()=>({}));if(body?.data?.subjectType==="customer")return`existing customer ${body.data.subjectId}`;}
 await nav(page,"Account");await page.getByPlaceholder("10-digit phone number").fill(PHONE);await page.getByRole("button",{name:"Send OTP"}).click();
 const sandbox=page.getByText(/Sandbox code \(no real SMS yet\):/i);await sandbox.waitFor({state:"visible",timeout:TIMEOUT});const code=(await sandbox.textContent())?.match(/\b(\d{6})\b/)?.[1];if(!code)die("sandbox OTP not rendered");
 await page.getByPlaceholder("6-digit code").fill(code);await page.getByPlaceholder("Your name (first time only)").fill(CUSTOMER);const verifyResponse=page.waitForResponse(response=>apiPath(response,"/api/customer-otp","POST"),{timeout:TIMEOUT});await page.getByRole("button",{name:"Verify & continue"}).click();const otp=await verifyResponse;if(!otp.ok())die(`OTP verify failed (HTTP ${otp.status()})`);await page.waitForFunction(()=>{try{return Boolean(JSON.parse(window.localStorage.getItem("pawspace_customer")||"null")?.customerId);}catch{return false;}},{},{timeout:TIMEOUT});
 const deadline=Date.now()+TIMEOUT;let verified,body;do{verified=await context.request.get(`${BASE}/api/identity-session`);body=await verified.json().catch(()=>({}));if(verified.ok()&&body?.data?.subjectType==="customer")break;await page.waitForTimeout(250);}while(Date.now()<deadline);if(!verified?.ok()||body?.data?.subjectType!=="customer")die(`OTP did not establish customer session (HTTP ${verified?.status()??0})`);return`real OTP -> ${body.data.subjectId}`;
}

async function ensurePet(page){
 await nav(page,"My Pets");if(await page.getByText(PET,{exact:true}).count())return`${PET} reused`;
 const add=page.getByRole("button",{name:/Add pet/i}).first();await add.waitFor({state:"visible",timeout:TIMEOUT});await add.click();await page.getByPlaceholder("Pet name").fill(PET);
 await page.getByLabel("Breed").selectOption({index:1});await page.getByLabel("Age").selectOption({index:1});await page.getByLabel("Weight").selectOption({index:1});await page.getByLabel("Temperament").selectOption({index:1});await page.getByLabel("Vaccinated?").selectOption("yes");
 const gender=page.getByLabel("Gender (optional)");if(await gender.count())await gender.selectOption({index:1});await page.getByRole("button",{name:"Add pet",exact:true}).click();await page.getByText(PET,{exact:true}).first().waitFor({state:"visible",timeout:TIMEOUT});return`${PET} created through UI`;
}

const care=(page)=>page.getByRole("heading",{name:"Everything they need",exact:true}).locator("xpath=ancestor::section[1]");
async function goHome(page){await gotoApp(page);await nav(page,"Home");await page.getByText("Everything they need",{exact:true}).waitFor({state:"visible",timeout:TIMEOUT});}
async function openService(page,name){await goHome(page);const card=care(page).getByRole("button",{name:new RegExp(name,"i")});const count=await card.count();if(count!==1)die(`${name} discovery card count=${count}`);if(await card.isDisabled())die(`${name} discovery card disabled`);await card.click();await wait(page,450);}

async function observeFinal(page,button,target,safePosts=[]){
 const seen=[],unexpected=[];const handler=async(route)=>{const req=route.request(),method=req.method(),path=new URL(req.url()).pathname,label=`${method} ${path}`;if(!["POST","PUT","PATCH","DELETE"].includes(method)){await route.continue();return;}if(target.test(label)){seen.push(label);await route.abort("blockedbyclient");return;}if(safePosts.some(pattern=>pattern.test(label))){await route.continue();return;}unexpected.push(label);await route.abort("blockedbyclient");};
 await page.route("**/api/**",handler);try{await button.click({timeout:TIMEOUT});const deadline=Date.now()+ASYNC_TIMEOUT;while(!seen.length&&!unexpected.length&&Date.now()<deadline)await page.waitForTimeout(250);}finally{await page.unroute("**/api/**",handler);}if(unexpected.length)die(`unexpected committing request(s) before target: ${unexpected.join(", ")}`);if(!seen.length)die(`expected final mutation ${target} not attempted within ${ASYNC_TIMEOUT}ms`);return seen.join(", ");
}

async function homeControls(page){
 await goHome(page);for(const name of["Grooming","Training","Boarding","Pet Sitting","Pet Taxi","Dog Walking","Fresh Food","Relocation"]){const card=care(page).getByRole("button",{name:new RegExp(name,"i")});if(await card.count()!==1)die(`${name} missing/duplicated`);if(await card.isDisabled())die(`${name} disabled`);}
 const guides=page.getByRole("heading",{name:"Quick service guides",exact:true}).locator("xpath=ancestor::section[1]").getByRole("button");if(await guides.count()!==6)die(`guide slots=${await guides.count()}, expected 6`);
 await page.getByRole("button",{name:"Choose your service location"}).click();await page.getByPlaceholder("e.g. HSR Layout, Bengaluru").fill("Koramangala, Bengaluru");await page.getByRole("button",{name:"Save location"}).click();await text(page,"Koramangala, Bengaluru");
 const search=page.getByLabel("Search PawSpace services");await search.fill("food");await care(page).getByRole("button",{name:/Fresh Food/i}).waitFor({state:"visible",timeout:TIMEOUT});if(await care(page).getByRole("button",{name:/Grooming/i}).count())die("search failed to filter service cards");await search.fill("");
 await page.getByRole("button",{name:/View your bookings/i}).click();await wait(page,200);await nav(page,"Home");await page.getByRole("button",{name:"Open pet profiles"}).click();await text(page,"Your pets");return"8 services + search + location + six guides + bookings + pets";
}

async function grooming(page){await openService(page,"Grooming");await text(page,"Who needs grooming?");const petText=page.getByText(PET,{exact:true}).first(),pet=petText.locator("xpath=ancestor::button[1]");await pet.waitFor({state:"visible",timeout:TIMEOUT});if((await pet.getAttribute("aria-pressed"))!=="true")await pet.click();const next=page.getByRole("button",{name:/Choose a package/i});await actionable(next,"Grooming package progression");await next.click();for(const label of["Essential Bath","Bath & Basic","Complete Makeover","Just Trim"])await text(page,label);return"pet -> package stage + legacy packages";}

async function training(page){
 await openService(page,"Training");await text(page,"Build better days together.");const plans=page.getByRole("button",{name:/See PawSpace plans/i});await actionable(plans,"Training plan progression");await plans.click();await text(page,"All training programmes");
 const trainerResponse=page.waitForResponse(response=>apiPath(response,"/api/training-trainers","GET"),{timeout:ASYNC_TIMEOUT}).catch(()=>null);await page.getByPlaceholder("Enter six-digit PIN code").first().fill(PIN);const trainers=await trainerResponse;if(!trainers)die("Training trainer lookup did not complete");if(!trainers.ok())die(`Training trainer lookup failed: ${await responseDetail(trainers)}`);const trainerBody=await trainers.json().catch(()=>({})),providerCount=Array.isArray(trainerBody?.data?.providers)?trainerBody.data.providers.length:0;
 const chooseTrainer=page.getByRole("button",{name:"Choose trainer"});await actionable(chooseTrainer,"Training trainer step");await chooseTrainer.click();await text(page,"Your trainer matches");if(providerCount===0){const unavailable=page.getByText(/No eligible trainer is currently available for this start date and zone/i);await unavailable.waitFor({state:"visible",timeout:TIMEOUT});if(RELEASE_PREVIEW)return"3 stages + governed trainer matching safely unavailable in release preview";die("Training trainer API returned no eligible providers");}
 const calendar=page.getByRole("button",{name:"Build session calendar"});await actionable(calendar,"Training eligible trainer",ASYNC_TIMEOUT);await calendar.click();await text(page,"Plan your sessions");await page.getByRole("button",{name:"Review & pay"}).click();await text(page,"Review your programme");const final=page.getByRole("button",{name:/request trainer approval|Refreshing server quote/i});await actionable(final,"Training final action",ASYNC_TIMEOUT);const seen=await observeFinal(page,final,/POST \/api\/uat-scheduling/,[/POST \/api\/training-commercial/]);return`5 stages + final scheduler wiring (${seen})`;
}

async function address(page){await page.getByLabel("Complete doorstep address").fill(ADDRESS);await page.getByPlaceholder("e.g., 560034").fill(PIN);await page.getByRole("button",{name:"Check",exact:true}).click();await text(page,"Indiranagar");}
async function stay(page,sitting){
 const name=sitting?"Pet Sitting":"Boarding";await openService(page,name);await text(page,sitting?"Care at home, around their routine.":"A stay that feels like home.");
 const commercialResponse=page.waitForResponse(response=>apiPath(response,sitting?"/api/sitting-commercial":"/api/boarding-commercial",sitting?"POST":"GET")&&(!sitting?new URL(response.url()).searchParams.has("scheduledStart"):true),{timeout:ASYNC_TIMEOUT}).catch(()=>null);await address(page);const commercial=await commercialResponse;if(!commercial)die(`${name} commercial availability/quote request did not complete`);if(!commercial.ok())die(`${name} commercial request failed: ${await responseDetail(commercial)}`);const commercialBody=await commercial.json().catch(()=>({}));
 let available=page.getByRole("button",{name:sitting?/See available sitters/i:/See available homes/i});await available.waitFor({state:"visible",timeout:TIMEOUT});if(await available.isDisabled()){const petText=page.getByText(PET,{exact:true}).first(),pet=petText.locator("xpath=ancestor::button[1]");await pet.waitFor({state:"visible",timeout:TIMEOUT});await pet.click();available=page.getByRole("button",{name:sitting?/See available sitters/i:/See available homes/i});}await actionable(available,`${name} stage 1`);await available.click();await text(page,sitting?"Choose your sitter":"Choose your host");
 if(!sitting){const hostCount=Array.isArray(commercialBody?.data?.hosts)?commercialBody.data.hosts.length:0;if(hostCount===0){const noHost=page.getByText(/No verified Boarding host currently has capacity for every selected pet in this UAT window/i);await noHost.waitFor({state:"visible",timeout:ASYNC_TIMEOUT});if(RELEASE_PREVIEW)return"2 stages + governed Boarding host matching safely unavailable in release preview";die("Boarding commercial API returned no eligible hosts");}}
 if(sitting){const rates=await page.locator("button").filter({hasText:"/ night"}).allTextContents();report.sittingProfileRateEvidence=rates.slice(0,3).map(x=>x.replace(/\s+/g," ").trim());if(!rates.length)die("Sitting profile rates not rendered for checkout-vs-card review");}
 const next=page.getByRole("button",{name:/Continue with/i});await actionable(next,`${name} eligible caregiver`,ASYNC_TIMEOUT);await next.click();await text(page,"Build the Care Card");await page.getByRole("button",{name:"Review protected booking"}).click();await text(page,"Review and confirm");const final=page.getByRole("button",{name:/create canonical stay|request final partner approval/i});try{await actionable(final,`${name} final action`,ASYNC_TIMEOUT);}catch(error){const alert=await page.getByRole("alert").last().textContent().catch(()=>"");die(`${error instanceof Error?error.message:String(error)}${alert?` — ${alert.trim()}`:""}`);}const seen=await observeFinal(page,final,sitting?/POST \/api\/(sitting-payment|uat-scheduling|canonical-bookings)/:/POST \/api\/uat-scheduling/,[/POST \/api\/(boarding|sitting).*quote/,/POST \/api\/live-price-quote/]);return`4 stages + caregiver + final wiring (${seen})`;
}

async function walking(page){
 await openService(page,"Dog Walking");await text(page,"Choose a walk package");const schedule=page.getByRole("button",{name:"Plan the schedule"});await actionable(schedule,"Walking catalogue");await schedule.click();await page.getByRole("button",{name:"Choose your dog"}).click();await text(page,"Who's walking?");const review=page.getByRole("button",{name:"Review & confirm"});if(await review.isDisabled())await page.getByText(PET,{exact:true}).first().click();await actionable(review,"Walking dog selection");await review.click();await text(page,"Review your walks");await page.getByPlaceholder("Enter six-digit PIN code").fill(PIN);const final=page.getByRole("button",{name:/Confirm \d+ walk|Refreshing server quote/i});await actionable(final,"Walking final action",ASYNC_TIMEOUT);const seen=await observeFinal(page,final,/POST \/api\/uat-scheduling/,[/POST \/api\/walking-commercial/]);return`4 stages + fresh quote + scheduler wiring (${seen})`;
}

async function taxi(page){
 await openService(page,"Pet Taxi");await text(page,"Choose a route class");const route=page.getByRole("button",{name:"Set pickup & drop"});await actionable(route,"Taxi catalogue");await route.click();await page.getByPlaceholder("e.g. Indiranagar, 100 Feet Road").fill("Koramangala");await page.getByPlaceholder("e.g. Whitefield vet clinic").fill("Indiranagar Vet Clinic");const choose=page.getByRole("button",{name:"Choose your pet"});await actionable(choose,"Taxi locations");await choose.click();const review=page.getByRole("button",{name:"Review & confirm"});if(await review.isDisabled())await page.getByText(PET,{exact:true}).first().click();await actionable(review,"Taxi pet selection");await review.click();await text(page,"Review your trip");await page.getByPlaceholder("Enter six-digit PIN code").fill(PIN);const final=page.getByRole("button",{name:/Confirm trip|Refreshing server quote/i});await actionable(final,"Taxi final action",ASYNC_TIMEOUT);const seen=await observeFinal(page,final,/POST \/api\/uat-scheduling/,[/POST \/api\/taxi-commercial/]);return`4 stages + fresh quote + scheduler wiring (${seen})`;
}

async function food(page){
 await openService(page,"Fresh Food");const flow=page.getByText("PAWSPACE FRESH FOOD",{exact:true}).locator("xpath=ancestor::section[1]");await flow.getByRole("heading",{name:"Fresh food for your pets"}).waitFor({state:"visible",timeout:TIMEOUT});await flow.getByPlaceholder("Enter six-digit PIN code").first().fill(FOOD_PIN);const coverage=flow.getByRole("button",{name:"Check service area & load catalogue"});await coverage.click();await flow.getByText("Delivery coverage confirmed",{exact:false}).waitFor({state:"visible",timeout:ASYNC_TIMEOUT});const add=flow.getByRole("button",{name:"Add",exact:true}).first();await actionable(add,"Food catalogue item",ASYNC_TIMEOUT);await add.click();const cart=flow.getByRole("button",{name:/Review cart/i});await actionable(cart,"Food cart");await cart.click();const plan=flow.getByRole("button",{name:"Choose delivery plan"});await actionable(plan,"Food delivery plan step");await plan.click();const details=flow.getByRole("button",{name:"Delivery details"});await actionable(details,"Food delivery details step");await details.click();await flow.getByRole("heading",{name:"Where and when?"}).waitFor({state:"visible",timeout:TIMEOUT});await flow.getByLabel("Delivery address").fill(ADDRESS);const review=flow.getByRole("button",{name:"Review with server quote"});await actionable(review,"Food server quote review",ASYNC_TIMEOUT);await review.click();await flow.getByRole("heading",{name:"Review and confirm"}).waitFor({state:"visible",timeout:ASYNC_TIMEOUT});const final=flow.getByRole("button",{name:/Confirm food order|Confirm order \+ repeat plan/i});await actionable(final,"Food final action",ASYNC_TIMEOUT);const seen=await observeFinal(page,final,/POST \/api\/food-orders/);return`5 stages + catalogue + quote + order wiring (${seen})`;
}

async function relocation(page){await openService(page,"Relocation");await text(page,"PET RELOCATION · ENQUIRY");await page.getByLabel("Email").fill("ui-acceptance@pawspace.test");await page.getByLabel("Pickup location").fill("Koramangala, Bengaluru");await page.getByLabel("Drop location").fill("Indiranagar, Bengaluru");const seen=await observeFinal(page,page.getByRole("button",{name:"Request relocation plan & quote"}),/POST \/api\/relocation-enquiry/);return`enquiry-only wiring (${seen}); no payment endpoint`;}

async function main(){const browser=await chromium.launch({headless:true}),context=await browser.newContext({viewport:{width:390,height:844}}),page=await context.newPage(),pageErrors=[];page.on("pageerror",error=>pageErrors.push(String(error.message).slice(0,240)));try{
 await runCase("real customer OTP/session",()=>login(page,context));await runCase("customer pet profile",()=>ensurePet(page));await runCase("premium Home and controls",()=>homeControls(page));await runCase("Grooming journey",()=>grooming(page));await runCase("Training journey",()=>training(page));await runCase("Boarding journey",()=>stay(page,false));await runCase("Pet Sitting journey",()=>stay(page,true));await runCase("Dog Walking journey",()=>walking(page));await runCase("Pet Taxi journey",()=>taxi(page));await runCase("Fresh Food journey",()=>food(page));await runCase("Relocation journey",()=>relocation(page));await runCase("no uncaught browser errors",async()=>{if(pageErrors.length)die(pageErrors.join(" | "));return"no pageerror events";});
 }finally{persist();await browser.close();}if(report.failures.length){console.error(`Customer UI acceptance failed: ${report.failures.length}`);process.exitCode=1;}else console.log(`Customer UI acceptance passed: ${report.summary.passed}/${report.summary.total}`);}
main().catch(error=>{report.failures.push({name:"harness",detail:error instanceof Error?error.message:String(error)});persist();console.error(error);process.exit(1);});
