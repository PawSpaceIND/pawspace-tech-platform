import type{MetadataRoute}from"next";import{publicServices}from"../lib/premium-public-content";import{breedGuides}from"../lib/breed-content";
const base="https://pawspace.in";
export default function sitemap():MetadataRoute.Sitemap{const now=new Date();return[{url:`${base}/discover`,lastModified:now,changeFrequency:"weekly",priority:1},{url:`${base}/services`,lastModified:now,changeFrequency:"weekly",priority:.9},{url:`${base}/about`,lastModified:now,changeFrequency:"monthly",priority:.6},{url:`${base}/contact`,lastModified:now,changeFrequency:"monthly",priority:.6},{url:`${base}/careers`,lastModified:now,changeFrequency:"monthly",priority:.5},{url:`${base}/locations/bengaluru/hsr-layout/pet-grooming`,lastModified:now,changeFrequency:"weekly",priority:.75},...publicServices.map(service=>({url:`${base}/services/${service.slug}`,lastModified:now,changeFrequency:"weekly" as const,priority:service.availability==="booking"?.8:.55})),...breedGuides.map(guide=>({url:`${base}/dog-breeds/${guide.slug}`,lastModified:now,changeFrequency:"monthly" as const,priority:.5}))]}
