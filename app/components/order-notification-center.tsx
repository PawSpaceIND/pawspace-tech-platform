"use client";
import{useEffect,useState}from"react";

type Item={id:string;service_code:string;event_type:string;severity:string;status:string;title:string;body:string;created_at:number};

export default function OrderNotificationCenter(){const[customerId,setCustomerId]=useState(""),[items,setItems]=useState<Item[]>([]),[open,setOpen]=useState(false);const unread=items.filter(item=>item.status==="unread").length;
 useEffect(()=>{let active=true;(async()=>{try{const session=await fetch("/api/identity-session",{cache:"no-store"}),body=await session.json() as{data?:{subjectType?:string;subjectId?:string}};if(active&&session.ok&&body.data?.subjectType==="customer"&&body.data.subjectId)setCustomerId(body.data.subjectId);}catch{}})();return()=>{active=false}},[]);
 useEffect(()=>{if(!customerId)return;let active=true;let timer:ReturnType<typeof setInterval>|undefined;const load=async()=>{try{const response=await fetch(`/api/order-notifications?customerId=${encodeURIComponent(customerId)}&limit=30`,{cache:"no-store"}),body=await response.json() as{data?:{items?:Item[]}};if(active&&response.ok)setItems(body.data?.items??[]);}catch{}};void load();timer=setInterval(()=>void load(),60_000);return()=>{active=false;if(timer)clearInterval(timer)}},[customerId]);
 if(!customerId)return null;
 async function markRead(item:Item){if(item.status==="read")return;try{const response=await fetch("/api/order-notifications",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({customerId,notificationId:item.id,action:"mark_read"})});if(response.ok)setItems(current=>current.map(value=>value.id===item.id?{...value,status:"read"}:value));}catch{}}
 return <div style={{position:"fixed",right:18,bottom:18,zIndex:80,fontFamily:"inherit"}}>{open&&<section role="dialog" aria-label="PawSpace order notifications" style={{position:"absolute",right:0,bottom:58,width:"min(360px,calc(100vw - 32px))",maxHeight:"65vh",overflow:"auto",background:"var(--ps-surface,#fff)",border:"1px solid var(--ps-border,#e7e1e8)",borderRadius:18,boxShadow:"0 18px 50px rgba(0,0,0,.18)",padding:12}}><header style={{display:"flex",justifyContent:"space-between",alignItems:"center",padding:"4px 4px 10px"}}><div><b>Order updates</b><small style={{display:"block",opacity:.65}}>{unread?`${unread} unread`:"You're all caught up"}</small></div><button aria-label="Close notifications" onClick={()=>setOpen(false)} style={{border:0,background:"transparent",fontSize:18}}>×</button></header>{items.length===0?<p style={{fontSize:13,opacity:.7,padding:8}}>New booking, service, cancellation and refund updates will appear here.</p>:items.map(item=><button key={item.id} onClick={()=>void markRead(item)} style={{display:"block",width:"100%",textAlign:"left",border:0,borderTop:"1px solid var(--ps-border,#eee)",background:item.status==="unread"?"rgba(111,72,172,.07)":"transparent",padding:"12px 8px",cursor:"pointer"}}><strong style={{display:"block",fontSize:13}}>{item.title}</strong><span style={{display:"block",fontSize:12,opacity:.75,lineHeight:1.45,marginTop:3}}>{item.body}</span><small style={{display:"block",fontSize:10,opacity:.55,marginTop:6}}>{new Date(item.created_at).toLocaleString("en-IN",{timeZone:"Asia/Kolkata"})} IST</small></button>)}</section>}<button aria-label={unread?`${unread} unread order updates`:"Order notifications"} onClick={()=>setOpen(value=>!value)} style={{width:52,height:52,borderRadius:26,border:"1px solid var(--ps-border,#ddd)",background:"var(--ps-surface,#fff)",boxShadow:"0 8px 26px rgba(0,0,0,.14)",fontSize:22,cursor:"pointer",position:"relative"}}>♢{unread>0&&<i style={{position:"absolute",right:-3,top:-3,minWidth:20,height:20,borderRadius:10,background:"#b3261e",color:"white",fontStyle:"normal",fontSize:11,display:"grid",placeItems:"center",padding:"0 3px"}}>{unread>99?"99+":unread}</i>}</button></div>;
}
