"use client";
import Link from"next/link";import{useState,useSyncExternalStore}from"react";import{progressTestTransaction,readTestLedger,readTestTransaction,subscribeTestTransaction,updateTestTransaction,type TestTransaction}from"../../lib/test-transaction";import styles from"./test-sync-panel.module.css";
type Surface="customer"|"crm"|"admin"|"provider"|"ops"|"accounts";
const surfaceLabels:Record<Surface,string>={customer:"Customer App",crm:"CRM",admin:"Admin OS",provider:"Provider App",ops:"Sales & Operations",accounts:"Accounts"};
const money=(value:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(value);
export default function TestSyncPanel({surface}:{surface:Surface}){
  const record=useSyncExternalStore(subscribeTestTransaction,readTestTransaction,()=>null);const[flash,setFlash]=useState("");const queueCount=readTestLedger().transactions.length;
  const act=(message:string,run:()=>TestTransaction|null)=>{run();setFlash(message);window.setTimeout(()=>setFlash(""),1800)};
  if(!record)return <section className={styles.empty}><div><span>TEST TRANSACTION ENGINE</span><b>No synchronized test booking yet</b><small>Create one from the 100-customer Test Lab. No live systems are connected.</small></div><Link href="/test-lab">Create test booking →</Link></section>;
  const terminal=["completed","cancelled"].includes(record.status);
  return <section className={styles.panel} data-surface={surface}><div className={styles.top}><div><span>SYNCED TEST RECORD · {surfaceLabels[surface]}</span><b>{record.id} · {record.customerName}</b><small>{record.pets} · {record.service} · {record.slot}</small></div><em>{record.status.replaceAll("_"," ")}</em></div><div className={styles.facts}><span><small>Provider</small><b>{record.provider}</b></span><span><small>Payment</small><b>{record.paymentStatus.replaceAll("_"," ")}</b></span><span><small>Value</small><b>{money(record.amount)}</b></span><span><small>Credits</small><b>{record.creditsAfter}</b></span></div><div className={styles.actions}>
    {surface==="customer"&&<><button disabled={terminal} onClick={()=>act("Test slot rescheduled",()=>updateTestTransaction({slot:"3:00–5:00 PM",status:"assigned",reminder:"Reschedule confirmation queued"},"customer","Customer rescheduled from the app"))}>Reschedule</button><button disabled={terminal} onClick={()=>act("Test booking cancelled",()=>updateTestTransaction({status:"cancelled",paymentStatus:"cancelled",crmNextAction:"Service recovery follow-up"},"customer","Customer cancelled; capacity and reserved credit released"))}>Cancel test</button></>}
    {surface==="crm"&&<><button onClick={()=>act("CRM task created",()=>updateTestTransaction({crmNextAction:"Call customer today · test task"},"crm","Sales/Ops follow-up task created"))}>Create follow-up</button><button onClick={()=>act("Cross-sell recorded",()=>updateTestTransaction({crmNextAction:"Offer fresh food trial"},"crm","Next-best offer changed to fresh food trial"))}>Add cross-sell</button></>}
    {surface==="admin"&&<><button disabled={terminal} onClick={()=>act("Test provider reassigned",()=>updateTestTransaction({provider:"Priya S.",providerModel:"Full-time",status:"assigned"},"admin","Operations reassigned the test booking"))}>Reassign</button><Link href="/test-lab">Open audit flow</Link></>}
    {surface==="provider"&&<><button disabled={terminal} onClick={()=>act("Job status synchronized",progressTestTransaction)}>{record.status==="awaiting_acceptance"?"Accept test job":record.status==="in_service"?"Complete service":"Advance status"}</button><button disabled={terminal} onClick={()=>act("Delay ticket synchronized",()=>updateTestTransaction({crmNextAction:"Operations delay ticket · urgent"},"provider","Provider reported a delay; customer update and Ops ticket queued"))}>Report delay</button></>}
    {surface==="ops"&&<><button disabled={terminal} onClick={()=>act("Operations follow-up synchronized",()=>updateTestTransaction({crmNextAction:"Operations review · test booking"},"ops","Sales/Ops review task added to the shared booking"))}>Add Ops review</button></>}
    {surface==="accounts"&&<><button disabled={terminal||record.paymentStatus==="paid"} onClick={()=>act("Test payment reconciled",()=>updateTestTransaction({paymentStatus:"paid"},"accounts","Accounts reconciled the test payment"))}>Mark test paid</button></>}
    <Link href="/test-lab">View test record</Link><Link href="/regression-lab">Queue: {queueCount}</Link>{flash&&<strong>{flash}</strong>}
  </div><div className={styles.foot}><span>Latest: {record.events[0]?.label}</span><small>Stored only in this test browser · live integrations 0</small></div></section>;
}
