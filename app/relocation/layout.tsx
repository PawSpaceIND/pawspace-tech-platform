import type { ReactNode } from "react";

export default function RelocationLayout({ children }: { children: ReactNode }) {
  return <div className="ps-unified-service ps-relocation">{children}</div>;
}
