"use client";
import Link from"next/link";
import{useEffect,useMemo,useState}from"react";
import{createCanonicalFoodOrder,createFoodQuote,loadFoodCatalogue,type FoodCatalogueItem,type FoodOrderResult,type FoodQuote}from"../../lib/food-client";
const customer={id:"TST-101",name:"PawSpace UAT Customer",primaryPhone:"+919880222741",secondaryPhone:"+919880222742",email:"uat.customer@pawspace.test"},box={background:"white",border:"1px solid #e4e4e4",borderRadius:16,padding:20} as const;
export default function CanonicalFoodPage(){const[items,setItems]=useState<FoodCatalogueItem[]>([]),[sku,setSku]=useState("food-uat-dog-adult-2kg"),[quantity,setQuantity]=useState(1),[quote,setQuote]=useState<FoodQuote|null>(null),[quoteLoading,setQuoteLoading]=useState(true),[quoteError,setQuoteError]=useState(""),[order,setOrder]=useState<FoodOrderResult|null>(null),[orderLoading,setOrderLoading]=useState(false),[orderError,setOrderError]=useState("");const active=useMemo(()=>items.find(item=>item.sku===sku),[items,sku]);useEffect(()=>{let cancelled=false;queueMicrotask(()=>{if(cancelled)return;setQuoteLoading(true);setQuoteError("");setQuote(null);setOrderError("")});Promise.all([loadFoodCatalogue(),createFoodQuote({sku,quantity,paymentMode:"sandbox_deferred"})]).then(([catalogue,nextQuote])=>{if(cancelled)return;setItems(catalogue.items);setQuote(nextQuote)}).catch(error=>{if(cancelled)return;setQuoteError(error instanceof Error?error.message:"Unable to refresh canonical Food quote")}).finally(()=>{if(!cancelled)setQuoteLoading(false)});return()=>{cancelled=true}},[sku,quantity]);async function orderNow(){if(!quote||orderLoading)return;setOrderLoading(true);setOrderError("");try{const result=await createCanonicalFoodOrder({idempotencyKey:`food:${quote.quoteId}:${customer.id}`,quoteId:quote.quoteId,customer});setOrder(result);window.scrollTo(0,0)}catch(error){setOrderError(error instanceof Error?error.message:"Unable to create canonical Food order")}finally{setOrderLoading(false)}}if(order)return <main style={{maxWidth:900,margin:"0 auto",padding:28,fontFamily:"system-ui",display:"grid",gap:16}}><Link href="/">PawSpace</Link><section style={box}><p>CANONICAL FOOD · INTERNAL UAT</p><h1>UAT inventory reserved</h1><h2>{order.orderId}</h2><p>{quote?.name} · {quote?.quantity} × ₹{quote?.unitPrice.toLocaleString("en-IN")}</p><div style={{display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:10}}><article style={box}><small>Order status</small><strong style={{display:"block"}}>{order.status}</strong><span>{order.deliveryStatus}</span></article><article style={box}><small>UAT value</small><strong style={{display:"block"}}>₹{order.totalAmount.toLocaleString("en-IN")}</strong><span>₹0 due now · live money disabled</span></article><article style={box}><small>Inventory</small><strong style={{display:"block"}}>{order.inventoryMode}</strong><span>Production inventory verified: {String(order.productionInventoryVerified)}</span></article></div><p>This is a canonical internal-UAT order reservation, not a production sale. Delivery fee policy, production inventory/SKU master, fulfilment logistics and live payment remain disconnected or configuration-pending.</p><button onClick={()=>setOrder(null)}>Back to UAT catalogue</button></section></main>;return <main style={{maxWidth:1080,margin:"0 auto",padding:28,fontFamily:"system-ui",display:"grid",gap:16,background:"#fafafa",minHeight:"100vh"}}><header><Link href="/">PawSpace</Link><p>PET FOOD · CANONICAL UAT</p><h1>Server-owned UAT catalogue</h1><p>Prices and stock below are explicit test catalogue/inventory values only. They do not represent a production SKU master, live warehouse stock or approved delivery-fee policy.</p></header><section style={{display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:12}}>{items.map(item=><button key={item.sku} onClick={()=>setSku(item.sku)} style={{...box,textAlign:"left",outline:sku===item.sku?"2px solid #222":"none"}}><strong>{item.name}</strong><p>{item.pack_size} · {item.pet_type}</p><b>₹{Number(item.unit_price).toLocaleString("en-IN")}</b><p><small>{item.uat_available_units} UAT units available · production inventory not verified</small></p></button>)}</section><section style={box}><h2>Quantity</h2><select value={quantity} onChange={e=>setQuantity(Number(e.target.value))}>{Array.from({length:Math.max(1,Math.min(5,Number(active?.max_qty_per_order||5)))},(_,i)=>i+1).map(value=><option key={value} value={value}>{value}</option>)}</select><p><small>Delivery fee is ₹0 in UAT because production delivery-fee policy is not configured. Coupons are disabled.</small></p></section><section style={{...box,position:"sticky",bottom:10,display:"flex",justifyContent:"space-between",alignItems:"center",gap:16}}><div><strong>{quote?`₹${quote.totalAmount.toLocaleString("en-IN")}`:"—"}</strong><div>{quoteLoading?"Refreshing canonical quote…":quoteError?quoteError:quote?`${quote.name} · ${quote.quantity} item(s) · ₹0 due now`:"Canonical quote unavailable"}</div></div><button disabled={!quote||quoteLoading||Boolean(quoteError)||orderLoading} onClick={()=>void orderNow()}>{orderLoading?"Reserving UAT stock…":"Reserve canonical UAT order →"}</button></section>{orderError&&<p role="alert">{orderError}</p>}</main>}
