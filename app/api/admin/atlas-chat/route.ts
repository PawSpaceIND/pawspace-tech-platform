import { authError, database, resolveActor, securityAudit } from "../../../../lib/server-auth";
import { answerFinancialQuestion } from "../../../../lib/intelligence/financial-query-agent";
import { executeAtlasApprovedAction, listAtlasMessages, recordAtlasMessage, requireFounderRole } from "../../../../lib/intelligence/atlas-data";

const json=(value:unknown,status=200)=>Response.json(value,{status,headers:{"cache-control":"no-store"}});
function sameOrigin(request:Request){const origin=request.headers.get("origin");if(origin&&origin!==new URL(request.url).origin)throw new Response("Cross-origin Atlas chat write blocked",{status:403});}
export async function GET(request:Request){try{const actor=requireFounderRole(await resolveActor(request)),db=await database(),limit=Number(new URL(request.url).searchParams.get("limit")||100);await securityAudit(db,actor,"atlas.chat.read","atlas_chat","recent","allowed",{limit});return json({data:{messages:await listAtlasMessages(db,limit),transport:"http_snapshot",websocketPath:"/api/admin/atlas-chat"}});}catch(error){return authError(error,"Unable to load Atlas chat");}}
export async function POST(request:Request){try{sameOrigin(request);const actor=requireFounderRole(await resolveActor(request)),db=await database(),body=await request.json() as Record<string,unknown>,action=String(body.action||"ask");if(action==="approve"){const messageId=String(body.messageId||"").trim();if(!messageId)return json({error:"Atlas proposal messageId is required"},400);await recordAtlasMessage(db,{role:"founder",actorEmail:actor.email,content:"Yes, execute.",replyToId:messageId});const data=await executeAtlasApprovedAction(db,{messageId,actorEmail:actor.email});await securityAudit(db,actor,"atlas.action.approve","atlas_chat_message",messageId,"completed",{status:data.status,explicitFounderApproval:true});return json({data});}if(action!=="ask")return json({error:"Unsupported Atlas action"},400);const question=String(body.message||"").trim();if(!question)return json({error:"Founder message is required"},400);await recordAtlasMessage(db,{role:"founder",actorEmail:actor.email,content:question});const data=await answerFinancialQuestion(db,question);const content=`I queried the isolated Tally analytics memory. ${data.rows.length} result row(s) matched. No live ledger was modified.`;const atlas=await recordAtlasMessage(db,{role:"atlas",actorEmail:"system:atlas",content});await securityAudit(db,actor,"atlas.financial.query","analytics_historical_financials",atlas.messageId,"completed",{sql:data.sql,rowCount:data.rows.length,readOnly:true});return json({data:{messageId:atlas.messageId,content,analytics:data}});}catch(error){return authError(error,"Unable to process Atlas chat");}}
