import{authError,database}from"../../../lib/server-auth";
import{recordCommunicationProviderCallback,signCommunicationProviderCallback}from"../../../lib/communication-provider-boundary";
import{INTERAKT_WHATSAPP_PROVIDER,isInteraktDeliveryWebhook}from"../../../lib/interakt-whatsapp";
import{recordInteraktDeliveryWebhookAtomic}from"../../../lib/interakt-delivery-atomic";
import{processInteraktInboundTrustSafe}from"../../../lib/trust-safety-interakt";
import{verifyCanonicalInteraktWebhook}from"../../../lib/interakt-webhook-auth";
import{captureInboundWebhook,runInboundWebhookAttempt}from"../../../lib/gateway-inbound-queue";

const json=(value:unknown,status=200)=>Response.json(value,{status,headers:{"cache-control":"no-store"}});
const MAX_CALLBACK_BYTES=65_536;
const isInteraktProvider=(provider:string)=>provider==="interakt"||provider===INTERAKT_WHATSAPP_PROVIDER;
const text=(value:unknown)=>String(value??"").trim();
function safeEqual(a:string,b:string){if(a.length!==b.length)return false;let diff=0;for(let i=0;i<a.length;i++)diff|=a.charCodeAt(i)^b.charCodeAt(i);return diff===0;}
function callbackEnvironment(env:Record<string,unknown>){const value=text(env.PAWSPACE_COMMUNICATION_ENV).toLowerCase();return value==="live"?"live":value==="uat"?"uat":"sandbox";}
async function verifyGeneric(rawBody:string,headers:Headers,env:Record<string,unknown>){const secret=text(env.PAWSPACE_COMMUNICATION_WEBHOOK_SECRET),timestamp=text(headers.get("x-pawspace-communication-timestamp")),signature=text(headers.get("x-pawspace-communication-signature")).toLowerCase(),stamp=Number(timestamp);if(!secret)return{ok:false,status:503,reason:"webhook_not_configured"};if(!timestamp||!Number.isFinite(stamp)||Math.abs(Date.now()-stamp)>5*60_000)return{ok:false,status:401,reason:"invalid_or_stale_timestamp"};const expected=await signCommunicationProviderCallback(secret,stamp,rawBody);return signature&&safeEqual(expected,signature)?{ok:true as const}:{ok:false as const,status:401,reason:"invalid_signature"};}
function identity(rawBody:string,provider:string){let body:Record<string,unknown>={};try{body=JSON.parse(rawBody)as Record<string,unknown>;}catch{return{eventId:"",messageId:null};}if(!isInteraktProvider(provider))return{eventId:text(body.event_id),messageId:text(body.provider_reference)||null};const data=body.data&&typeof body.data==="object"?body.data as Record<string,unknown>:{},message=data.message&&typeof data.message==="object"?data.message as Record<string,unknown>:{},providerMessageId=text(message.id)||text(data.id),eventId=text(body.event_id)||text(body.id)||[text(body.type),providerMessageId,text(body.timestamp)].filter(Boolean).join(":");return{eventId,messageId:providerMessageId||null};}
async function processCallback(db:D1Database,env:Record<string,unknown>,provider:string,rawBody:string,headers:Headers){if(isInteraktProvider(provider))return isInteraktDeliveryWebhook(rawBody)?recordInteraktDeliveryWebhookAtomic(db,env,{rawBody,headers}):processInteraktInboundTrustSafe(db,env,{rawBody,headers});return recordCommunicationProviderCallback(db,env,{provider,rawBody,headers});}

export async function POST(request:Request){try{
 const provider=text(new URL(request.url).searchParams.get("provider")||request.headers.get("x-pawspace-communication-provider")).toLowerCase();if(!provider)return json({error:"Communication provider is required"},400);const declared=Number(request.headers.get("content-length")||0);if(Number.isFinite(declared)&&declared>MAX_CALLBACK_BYTES)return json({error:"Provider callback payload is too large"},413);const rawBody=await request.text();if(new TextEncoder().encode(rawBody).byteLength>MAX_CALLBACK_BYTES)return json({error:"Provider callback payload is too large"},413);
 const{env}=await import("cloudflare:workers"),runtime=env as unknown as Record<string,unknown>,db=await database();const verified=isInteraktProvider(provider)?await verifyCanonicalInteraktWebhook(rawBody,request.headers,runtime):await verifyGeneric(rawBody,request.headers,runtime);if(!verified.ok)return json({error:verified.reason},verified.status);
 const ids=identity(rawBody,provider);if(!ids.eventId)return json({error:"Provider callback event ID is required"},400);
 const captured=await captureInboundWebhook(db,{provider,routeKey:"communication-provider-callback",environment:callbackEnvironment(runtime),eventId:ids.eventId,messageId:ids.messageId,rawBody,headers:request.headers});const status=text(captured.row.status);if(status==="PROCESSED")return json({ok:true,duplicatePrevented:true,status});if(status==="DEAD_LETTER")return json({ok:false,duplicatePrevented:true,status},503);
 const attempt=await runInboundWebhookAttempt(db,{queueId:text(captured.row.id),workerId:`communication-webhook:${crypto.randomUUID()}`,handler:async({rawBody})=>{const result=await processCallback(db,runtime,provider,rawBody,request.headers);if(!result.accepted)throw new Response(String(result.reason||"provider callback rejected"),{status:Number(result.status||500)});return result;}});if(!attempt.claimed)return json({ok:true,queued:true,duplicatePrevented:true,status},202);if(!attempt.ok){const code=attempt.error instanceof Response?attempt.error.status:503;return json({ok:false,queued:true,status:attempt.failure.status,error:attempt.error instanceof Response?await attempt.error.text():"Provider callback processing failed"},code>=400&&code<500?code:503);}
 const result=attempt.result as Record<string,unknown>;return json({ok:true,...result,queued:true,duplicatePrevented:captured.duplicatePrevented},Number(result.status||200));
}catch(error){return authError(error,"Unable to process communication provider callback");}}
