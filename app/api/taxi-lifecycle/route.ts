import{authError,database,requireCustomerOwnership,requirePermission,requireProviderOwnership,resolveActor,securityAudit}from"../../../lib/server-auth";
import{hasPermission}from"../../../lib/platform-security";
import{getTaxiBooking,listTaxiBookings,mutateTaxiBooking,type TaxiAction}from"../../../lib/taxi-lifecycle";
type Body={bookingId?:string;action?:TaxiAction;idempotencyKey?:string;vehicleId?:string;reason?:string;handoverMethod?:"owner"|"building_staff"|"clinic_staff";tripEventType?:string;detail?:Record<string,unknown>};const json=(value:unknown,status=200)=>Response.json(value,{status,headers:{"cache-control":"no-store"}}),providerActions=new Set<TaxiAction>(["accept","decline","assign_vehicle","confirm_pickup","start_trip","trip_event","arrive_dropoff","confirm_dropoff","complete_trip","driver_unavailable"]);
export async function GET(request:Request){try{const url=new URL(request.url),providerId=String(url.searchParams.get("providerId")||"").trim(),customerId=String(url.searchParams.get("customerId")||"").trim(),bookingId=String(url.searchParams.get("bookingId")||"").trim(),customerScope=url.searchParams.get("scope")==="customer",db=await database(),actor=await resolveActor(request);if(customerScope){requirePermission(actor,"scheduling.book");if(!bookingId||providerId||customerId)return json({error:"Customer Pet Taxi reads require only a booking ID"},400);const rows=await listTaxiBookings(db,{bookingId});if(!rows.length)return json({data:[]});await requireCustomerOwnership(db,actor,String(rows[0].customer_id));return json({data:rows})}if(!providerId&&!customerId&&!bookingId)return json({error:"Pet Taxi lifecycle reads require provider, customer or booking scope"},400);requirePermission(actor,"bookings.view");if(providerId)await requireProviderOwnership(db,actor,providerId);if(customerId)await requireCustomerOwnership(db,actor,customerId);if(bookingId&&!hasPermission(actor.permissions,"bookings.manage")){const booking=await getTaxiBooking(db,bookingId);try{await requireProviderOwnership(db,actor,String(booking.provider_id))}catch{await requireCustomerOwnership(db,actor,String(booking.customer_id))}}return json({data:await listTaxiBookings(db,{providerId:providerId||undefined,customerId:customerId||undefined,bookingId:bookingId||undefined})})}catch(error){return authError(error,"Unable to load Pet Taxi lifecycle")}}
export async function POST(request:Request){try{const body=await request.json() as Body,bookingId=String(body.bookingId||"").trim(),action=body.action,idempotencyKey=String(body.idempotencyKey||"").trim();if(!bookingId||!action||!idempotencyKey)return json({error:"Booking, action and idempotency key are required"},400);const db=await database(),actor=await resolveActor(request),booking=await getTaxiBooking(db,bookingId),providerId=String(booking.provider_id),staff=hasPermission(actor.permissions,"bookings.manage");if(action==="no_show")requirePermission(actor,"bookings.manage");else if(providerActions.has(action)){requirePermission(actor,"bookings.view");if(!staff)await requireProviderOwnership(db,actor,providerId)}else return json({error:"Unsupported Pet Taxi lifecycle action"},400);const result=await mutateTaxiBooking(db,{bookingId,action,actorId:actor.email,idempotencyKey,vehicleId:body.vehicleId,reason:body.reason,handoverMethod:body.handoverMethod,tripEventType:body.tripEventType,detail:body.detail});await securityAudit(db,actor,`taxi.lifecycle.${action}`,"canonical_booking",bookingId,"completed",{providerId,staffOverride:staff,duplicatePrevented:Boolean((result as Record<string,unknown>).duplicatePrevented)});return json({data:result},String((result as Record<string,unknown>).status)==="ops_escalation"?202:200)}catch(error){return authError(error,"Unable to update Pet Taxi lifecycle")}}
