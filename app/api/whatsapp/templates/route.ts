import{authError,authorize,database,securityAudit}from"../../../../lib/server-auth";import{listWhatsAppTemplateLifecycle,pauseWhatsAppTemplate,saveWhatsAppTemplateDraft,submitWhatsAppTemplate}from"../../../../lib/whatsapp-template-lifecycle";import{verifyWhatsAppTemplateAgainstMeta}from"../../../../lib/meta-whatsapp-template-verification";
type Body={action?:string;templateKey?:string;displayName?:string;category?:string;language?:string;body?:string;sampleValues?:unknown[];reason?:string};const json=(v:unknown,s=200)=>Response.json(v,{status:s,headers:{"cache-control":"no-store"}});function sameOrigin(r:Request){const o=r.headers.get("origin");if(o&&o!==new URL(r.url).origin)throw new Response("Cross-origin WhatsApp template write blocked",{status:403});}async function runtimeEnv(){const{env}=await import("cloudflare:workers");return env as unknown as Record<string,unknown>;}
export async function GET(request:Request){try{await authorize(request,"communications.manage");return json({data:await listWhatsAppTemplateLifecycle(await database())})}catch(error){if(error instanceof Response)return json({error:await error.text()},error.status);return authError(error,"Unable to load WhatsApp templates")}}
export async function POST(request:Request){try{sameOrigin(request);const actor=await authorize(request,"communications.manage"),db=await database(),body=await request.json()as Body,action=String(body.action||"").trim(),templateKey=String(body.templateKey||"").trim();if(action==="save_draft"){const data=await saveWhatsAppTemplateDraft(db,{templateKey,displayName:String(body.displayName||""),category:String(body.category||""),language:String(body.language||""),body:String(body.body||""),sampleValues:body.sampleValues,actorEmail:actor.email});await securityAudit(db,actor,"whatsapp.template.draft_saved","whatsapp_template",templateKey,"completed",{});return json({data},201)}if(action==="submit"){const data=await submitWhatsAppTemplate(db,{templateKey,actorEmail:actor.email,reason:String(body.reason||"")});return json({data})}if(action==="verify_meta"){const data=await verifyWhatsAppTemplateAgainstMeta(db,await runtimeEnv(),{templateKey,actorEmail:actor.email});await securityAudit(db,actor,"whatsapp.template.meta_verified","whatsapp_template",templateKey,"completed",{providerStatus:data.providerStatus,metaReference:data.metaReference,externalMetaMutation:false});return json({data})}if(action==="pause")return json({data:await pauseWhatsAppTemplate(db,{templateKey,actorEmail:actor.email,reason:String(body.reason||"")})});return json({error:"Supported actions are save_draft, submit, verify_meta or pause"},400)}catch(error){if(error instanceof Response)return json({error:await error.text()},error.status);return authError(error,"Unable to update WhatsApp template lifecycle")}}
