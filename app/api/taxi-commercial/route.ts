import{redactUnexpected}from"../../../lib/governed-http-error";
import{database}from"../../../lib/server-auth";
import{createTaxiQuote,listTaxiRouteClasses,type TaxiPaymentMode}from"../../../lib/taxi-governance";
const json=(value:unknown,status=200)=>Response.json(value,{status,headers:{"cache-control":"no-store"}});
function sameOriginWrite(request:Request){const origin=request.headers.get("origin");if(origin&&origin!==new URL(request.url).origin)throw new Response("Cross-origin Pet Taxi quote blocked",{status:403});}
async function failure(error:unknown){if(error instanceof Response){const message=await error.text().catch(()=>"");return json({error:message||"Pet Taxi commercial request failed"},error.status||500)}return json({error:redactUnexpected(error,"Pet Taxi commercial request failed")},500)}
export async function GET(request:Request){try{const scheduledStart=new URL(request.url).searchParams.get("scheduledStart")||new Date().toISOString(),routes=await listTaxiRouteClasses(await database(),scheduledStart);return json({data:{routes,source:"canonical_taxi_governance",routeSource:"uat_route_class",productionMapsVerified:false,liveAvailability:false,liveMoney:false}})}catch(error){return failure(error)}}
export async function POST(request:Request){try{sameOriginWrite(request);const body=await request.json() as {routeCode?:string;originLabel?:string;destinationLabel?:string;petCount?:number;scheduledStart?:string;paymentMode?:TaxiPaymentMode;couponCode?:string};if(!body.routeCode||!body.originLabel||!body.destinationLabel||!body.scheduledStart)return json({error:"Pet Taxi route, pickup, drop-off and pickup time are required"},400);const quote=await createTaxiQuote(await database(),{routeCode:body.routeCode,originLabel:body.originLabel,destinationLabel:body.destinationLabel,petCount:Number(body.petCount||0),scheduledStart:body.scheduledStart,paymentMode:body.paymentMode||"sandbox_deferred",couponCode:body.couponCode});return json({data:{...quote,liveMoney:false}},201)}catch(error){return failure(error)}}