import{refuseUnlessGatewayPermits}from"../../../lib/api-gateway";
import{authError,database,requireCustomerOwnership,requirePermission,resolveActor,securityAudit}from"../../../lib/server-auth";
import{createFoodSubscription,getFoodSubscriptionSnapshot,processDueFoodSubscriptionRenewals,recordFoodSubscriptionRenewalPayment,setFoodSubscriptionStatus,type FoodSubscriptionChannel}from"../../../lib/food-subscription-governance";
type Row=Record<string,unknown>;
const json=(value:unknown,status=200)=>Response.json(value,{status,headers:{"cache-control":"no-store"}});
async function ownerForSnapshot(db:D1Database,snapshot:Awaited<ReturnType<typeof getFoodSubscriptionSnapshot>>,actor:Awaited<ReturnType<typeof resolveActor>>){if(!snapshot)throw new Response("Food subscription not found",{status:404});await requireCustomerOwnership(db,actor,String(snapshot.subscription.customer_id));}
export async function GET(request:Request){const denied=await refuseUnlessGatewayPermits(request);if(denied)return denied;try{const url=new URL(request.url),subscriptionId=String(url.searchParams.get("subscriptionId")||""),renewalId=String(url.searchParams.get("renewalId")||""),invoiceId=String(url.searchParams.get("invoiceId")||"");if(!subscriptionId&&!renewalId&&!invoiceId)return json({error:"Subscription, renewal or invoice ID is required"},400);const db=await database(),actor=await resolveActor(request);requirePermission(actor,"scheduling.book");const snapshot=await getFoodSubscriptionSnapshot(db,{subscriptionId:subscriptionId||undefined,renewalId:renewalId||undefined,invoiceId:invoiceId||undefined});await ownerForSnapshot(db,snapshot,actor);return json({data:snapshot});}catch(error){return authError(error,"Unable to load Food subscription")}}
export async function POST(request:Request){const denied=await refuseUnlessGatewayPermits(request);if(denied)return denied;try{const body=await request.json() as Record<string,unknown>,action=String(body.action||""),db=await database(),actor=await resolveActor(request);if(action==="create"){requirePermission(actor,"scheduling.book");const sourceOrderId=String(body.sourceOrderId||"").trim(),interval=Math.floor(Number(body.renewalIntervalDays||0)),channel=String(body.communicationChannel||"whatsapp") as FoodSubscriptionChannel;if(!sourceOrderId)return json({error:"Source Food order is required"},400);const order=await db.prepare("SELECT customer_id FROM food_orders WHERE id=?").bind(sourceOrderId).first<Row>();if(!order)return json({error:"Canonical Food source order not found"},404);await requireCustomerOwnership(db,actor,String(order.customer_id));const result=await createFoodSubscription(db,{sourceOrderId,customerId:String(order.customer_id),renewalIntervalDays:interval,communicationChannel:channel,firstRenewalAt:body.firstRenewalAt==null?undefined:Number(body.firstRenewalAt),actorId:actor.email});await securityAudit(db,actor,"food.subscription.create","food_order",sourceOrderId,"completed",{subscriptionId:(result as Record<string,unknown>).subscriptionId,autoCharge:false});return json({data:result},201)}if(["pause","resume","cancel"].includes(action)){requirePermission(actor,"scheduling.book");const subscriptionId=String(body.subscriptionId||"").trim(),snapshot=await getFoodSubscriptionSnapshot(db,{subscriptionId});await ownerForSnapshot(db,snapshot,actor);const result=await setFoodSubscriptionStatus(db,{subscriptionId,status:action==="pause"?"paused":action==="resume"?"active":"cancelled",actorId:actor.email,reason:String(body.reason||action)});await securityAudit(db,actor,`food.subscription.${action}`,"food_subscription",subscriptionId,"completed",{reason:String(body.reason||action)});return json({data:result})}if(action==="process_due"){requirePermission(actor,"finance.manage");const result=await processDueFoodSubscriptionRenewals(db,{actorId:actor.email,subscriptionId:body.subscriptionId?String(body.subscriptionId):undefined,at:body.at?Number(body.at):undefined});await securityAudit(db,actor,"food.subscription.process_due","food_subscription",String(body.subscriptionId||"due_batch"),"completed",{processed:result.processed,schedulerConnected:false,autoCharge:false});return json({data:result})}if(action==="record_payment"){requirePermission(actor,"finance.manage");const renewalId=String(body.renewalId||"").trim(),paymentReference=String(body.paymentReference||"").trim();if(!renewalId||!paymentReference)return json({error:"Renewal and canonical payment reference are required"},400);const result=await recordFoodSubscriptionRenewalPayment(db,{renewalId,paymentReference,actorId:actor.email});await securityAudit(db,actor,"food.subscription.record_payment","food_subscription_renewal",renewalId,"completed",{invoiceId:result.invoiceId,confirmationQueued:true,invoiceQueued:true,liveMoney:false});return json({data:result})}return json({error:"Unsupported Food subscription action"},400)}catch(error){return authError(error,"Unable to update Food subscription")}}
