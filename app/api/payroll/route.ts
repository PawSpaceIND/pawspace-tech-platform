import{authError,authorize,database,securityAudit}from"../../../lib/server-auth";
import{approvePayroll,assignCompensation,calculatePayroll,payrollDirectory,prepareSandboxPaymentBatch,reviewPayroll,saveSalaryStructure}from"../../../lib/payroll-engine";

type Row=Record<string,unknown>;
const text=(v:unknown)=>String(v??"").trim();
export async function GET(request:Request){try{await authorize(request,"payroll.view");const db=await database();return Response.json({data:await payrollDirectory(db),productionReady:false});}catch(error){return authError(error,"Unable to load payroll");}}
export async function POST(request:Request){try{const body=await request.json() as Row,action=text(body.action),db=await database();if(action==="approve"){const actor=await authorize(request,"payroll.approve");await approvePayroll(db,{runId:text(body.runId),actorId:actor.email});await securityAudit(db,actor,"payroll.approve","payroll_run",text(body.runId),"completed");return Response.json({data:{runId:text(body.runId),status:"approved"},productionReady:false});}const permission=action==="save_structure"||action==="assign_compensation"?"compensation.manage":"payroll.manage",actor=await authorize(request,permission);let result:unknown;if(action==="save_structure")result=await saveSalaryStructure(db,{structureCode:text(body.structureCode),effectiveFrom:Number(body.effectiveFrom),components:Array.isArray(body.components)?body.components as never[]:[],actorId:actor.email,approvalReference:text(body.approvalReference)||null});else if(action==="assign_compensation")result=await assignCompensation(db,{employeeId:text(body.employeeId),structureId:text(body.structureId),effectiveFrom:Number(body.effectiveFrom),reason:text(body.reason),actorId:actor.email});else if(action==="calculate")result=await calculatePayroll(db,{periodStart:Number(body.periodStart),periodEnd:Number(body.periodEnd),idempotencyKey:text(body.idempotencyKey),actorId:actor.email});else if(action==="review"){await reviewPayroll(db,{runId:text(body.runId),actorId:actor.email});result={runId:text(body.runId),status:"reviewed"};}else if(action==="prepare_payment")result=await prepareSandboxPaymentBatch(db,{runId:text(body.runId),actorId:actor.email});else return Response.json({error:"Unknown payroll action"},{status:400});await securityAudit(db,actor,`payroll.${action}`,action.includes("structure")?"salary_structure":"payroll_run",text(body.runId)||null,"completed");return Response.json({data:result,productionReady:false});}catch(error){return authError(error,"Payroll update failed");}}
