import{authError,authorize,database,securityAudit}from"../../../lib/server-auth";
import{aiBusinessConfigurationSnapshot,createAiBusinessDraft,retrieveApprovedKnowledge,setAiKillSwitch,transitionAiBusinessConfig,type AiConfigEntityType,type AiConfigLifecycleAction,type AiKillSwitchScope}from"../../../lib/ai-business-configuration";

type Body={action?:string;entityType?:AiConfigEntityType;entityId?:string;lifecycleAction?:AiConfigLifecycleAction;payload?:Record<string,unknown>;scopeType?:AiKillSwitchScope;scopeKey?:string;disabled?:boolean;reason?:string;query?:string;visibilityScopes?:string[]};
const json=(value:unknown,status=200)=>Response.json(value,{status,headers:{"cache-control":"no-store"}});
function sameOrigin(request:Request){const origin=request.headers.get("origin");if(origin&&origin!==new URL(request.url).origin)throw new Response("Cross-origin AI business configuration write blocked",{status:403});}

export async function GET(request:Request){try{await authorize(request,"settings.manage");const db=await database(),url=new URL(request.url),mode=url.searchParams.get("mode");if(mode==="retrieve"){const query=url.searchParams.get("query")||"",scopes=(url.searchParams.get("scopes")||"public").split(",").map(item=>item.trim()).filter(Boolean);if(!query)return json({error:"Knowledge query is required"},400);return json({data:await retrieveApprovedKnowledge(db,{query,visibilityScopes:scopes})});}return json({data:await aiBusinessConfigurationSnapshot(db)});}catch(error){return authError(error,"Unable to load AI business configuration");}}

export async function POST(request:Request){try{sameOrigin(request);const actor=await authorize(request,"settings.manage"),db=await database(),body=await request.json() as Body;if(body.action==="create_draft"){if(!body.entityType||!body.payload)return json({error:"Entity type and payload are required"},400);const data=await createAiBusinessDraft(db,{entityType:body.entityType,payload:body.payload,actorEmail:actor.email});await securityAudit(db,actor,"ai.config.draft","ai_configuration",data.id,"completed",{entityType:body.entityType,version:data.version});return json({data},201);}if(body.action==="transition"){if(!body.entityType||!body.entityId||!body.lifecycleAction)return json({error:"Entity type, entity ID and lifecycle action are required"},400);const data=await transitionAiBusinessConfig(db,{entityType:body.entityType,entityId:body.entityId,action:body.lifecycleAction,actorEmail:actor.email});await securityAudit(db,actor,`ai.config.${body.lifecycleAction}`,"ai_configuration",body.entityId,"completed",{entityType:body.entityType});return json({data});}if(body.action==="kill_switch"){if(!body.scopeType||!body.scopeKey)return json({error:"Kill-switch scope is required"},400);const data=await setAiKillSwitch(db,{scopeType:body.scopeType,scopeKey:body.scopeKey,disabled:Boolean(body.disabled),reason:body.reason||"",actorEmail:actor.email});await securityAudit(db,actor,"ai.config.kill_switch","ai_kill_switch",`${body.scopeType}:${body.scopeKey}`,"completed",{disabled:Boolean(body.disabled),reason:body.reason||null});return json({data});}if(body.action==="retrieve_knowledge"){const query=body.query||"",scopes=Array.isArray(body.visibilityScopes)?body.visibilityScopes:[];if(!query||!scopes.length)return json({error:"Knowledge query and visibility scopes are required"},400);const data=await retrieveApprovedKnowledge(db,{query,visibilityScopes:scopes});await securityAudit(db,actor,"ai.knowledge.retrieve","ai_knowledge",null,"completed",{approvedCurrentOnly:true,resultCount:data.results.length});return json({data});}return json({error:"Unsupported AI business configuration action"},400);}catch(error){if(error instanceof Response)return json({error:await error.text()},error.status);return authError(error,"Unable to update AI business configuration");}}
