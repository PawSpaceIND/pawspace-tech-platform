import{authError,authorize,securityAudit}from"../../../lib/server-auth";
import{ensureGroomingPolicyTables,seedDefaultGroomingPolicy}from"../../../lib/grooming-policy-governance";

type Row=Record<string,unknown>;
const json=(value:unknown,status=200)=>Response.json(value,{status});
async function database(){const{env}=await import("cloudflare:workers");return env.DB;}
const editable=["enforcement_mode","cancellation_cutoff_minutes","refund_percent_before_cutoff","refund_percent_after_cutoff","reschedule_cutoff_minutes","reschedule_allowed_after_cutoff","max_reschedules","reschedule_fee_type","reschedule_fee_value","no_show_refund_percent","multi_pet_max","multi_pet_pricing_mode","change_lock_statuses_json","active","effective_from","effective_to"];

export async function GET(request:Request){try{await authorize(request,"pricing.view");const db=await database();await seedDefaultGroomingPolicy(db);const url=new URL(request.url),cityId=(url.searchParams.get("cityId")||"blr").trim();const rows=await db.prepare("SELECT * FROM grooming_commercial_policies WHERE city_id=? ORDER BY policy_code,version DESC").bind(cityId).all<Row>();const audit=await db.prepare("SELECT * FROM grooming_commercial_policy_audit WHERE city_id=? ORDER BY created_at DESC LIMIT 50").bind(cityId).all<Row>();return json({data:{cityId,policies:rows.results,audit:audit.results}});}catch(error){return authError(error,"Unable to load Grooming commercial policy");}}

export async function PATCH(request:Request){try{const actor=await authorize(request,"pricing.manage");const db=await database();await seedDefaultGroomingPolicy(db);const body=await request.json() as {id?:string;changes?:Record<string,unknown>;reason?:string};if(!body.id||!body.reason||body.reason.trim().length<5)return json({error:"Policy ID and a clear change reason are required"},400);const before=await db.prepare("SELECT * FROM grooming_commercial_policies WHERE id=?").bind(body.id).first<Row>();if(!before)return json({error:"Grooming commercial policy not found"},404);const entries=Object.entries(body.changes||{}).filter(([key])=>editable.includes(key));if(!entries.length)return json({error:"No supported policy changes supplied"},400);for(const [key,value] of entries){if(["cancellation_cutoff_minutes","refund_percent_before_cutoff","refund_percent_after_cutoff","reschedule_cutoff_minutes","max_reschedules","reschedule_fee_value","no_show_refund_percent","multi_pet_max"].includes(key)&&Number(value)<0)return json({error:`${key} cannot be negative`},400);if(["refund_percent_before_cutoff","refund_percent_after_cutoff","no_show_refund_percent"].includes(key)&&Number(value)>100)return json({error:`${key} cannot exceed 100`},400);if(key==="enforcement_mode"&&!['observe','enforce'].includes(String(value)))return json({error:"Enforcement mode must be observe or enforce"},400);if(key==="reschedule_fee_type"&&!['none','flat','percent'].includes(String(value)))return json({error:"Reschedule fee type must be none, flat or percent"},400);if(key==="multi_pet_pricing_mode"&&!['catalogue','per_pet','custom'].includes(String(value)))return json({error:"Multi-pet pricing mode must be catalogue, per_pet or custom"},400);}const now=Date.now(),set=entries.map(([key])=>`${key}=?`).join(",");await db.prepare(`UPDATE grooming_commercial_policies SET ${set},version=version+1,updated_by=?,updated_at=? WHERE id=?`).bind(...entries.map(([,value])=>value),actor.email,now,body.id).run();const after=await db.prepare("SELECT * FROM grooming_commercial_policies WHERE id=?").bind(body.id).first<Row>();await db.prepare("INSERT INTO grooming_commercial_policy_audit (id,policy_id,policy_code,city_id,action,before_json,after_json,actor_id,reason,created_at) VALUES (?,?,?,?,?,?,?,?,?,?)").bind(crypto.randomUUID(),body.id,String(before.policy_code),String(before.city_id),"updated",JSON.stringify(before),JSON.stringify(after),actor.email,body.reason.trim(),now).run();await securityAudit(db,actor,"grooming.commercial_policy.update","commercial_policy",body.id,"completed",{reason:body.reason,changes:entries.map(([key])=>key)});return json({data:after});}catch(error){return authError(error,"Unable to update Grooming commercial policy");}}

export async function POST(request:Request){try{const actor=await authorize(request,"pricing.manage");const db=await database();await ensureGroomingPolicyTables(db);const body=await request.json() as Record<string,unknown>;if(!body.cityId||!body.policyCode)return json({error:"City and policy code are required"},400);const id=`gpolicy_${crypto.randomUUID().slice(0,12)}`,now=Date.now();await db.prepare("INSERT INTO grooming_commercial_policies (id,policy_code,city_id,zone_id,enforcement_mode,cancellation_cutoff_minutes,refund_percent_before_cutoff,refund_percent_after_cutoff,reschedule_cutoff_minutes,reschedule_allowed_after_cutoff,max_reschedules,reschedule_fee_type,reschedule_fee_value,no_show_refund_percent,multi_pet_max,multi_pet_pricing_mode,change_lock_statuses_json,active,version,effective_from,effective_to,updated_by,updated_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,1,1,?,?,?,?)").bind(id,String(body.policyCode),String(body.cityId),body.zoneId?String(body.zoneId):null,String(body.enforcementMode||"observe"),Number(body.cancellationCutoffMinutes||0),Number(body.refundPercentBeforeCutoff??100),Number(body.refundPercentAfterCutoff??100),Number(body.rescheduleCutoffMinutes||0),body.rescheduleAllowedAfterCutoff===false?0:1,Number(body.maxReschedules||0),String(body.rescheduleFeeType||"none"),Number(body.rescheduleFeeValue||0),Number(body.noShowRefundPercent||0),Number(body.multiPetMax||4),String(body.multiPetPricingMode||"catalogue"),JSON.stringify(body.changeLockStatuses||["completed","cancelled"]),String(body.effectiveFrom||new Date().toISOString().slice(0,10)),body.effectiveTo?String(body.effectiveTo):null,actor.email,now).run();const created=await db.prepare("SELECT * FROM grooming_commercial_policies WHERE id=?").bind(id).first<Row>();await db.prepare("INSERT INTO grooming_commercial_policy_audit (id,policy_id,policy_code,city_id,action,before_json,after_json,actor_id,reason,created_at) VALUES (?,?,?,?,?,NULL,?,?,?,?)").bind(crypto.randomUUID(),id,String(body.policyCode),String(body.cityId),"created",JSON.stringify(created),actor.email,String(body.reason||"New Grooming commercial policy"),now).run();await securityAudit(db,actor,"grooming.commercial_policy.create","commercial_policy",id,"completed",{cityId:body.cityId,zoneId:body.zoneId??null});return json({data:created},201);}catch(error){return authError(error,"Unable to create Grooming commercial policy");}}
