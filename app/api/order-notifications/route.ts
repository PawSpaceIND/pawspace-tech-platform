import{authError,database,requireCustomerOwnership,resolveActor}from"../../../lib/server-auth";
import{listOrderNotifications,markOrderNotificationRead,runOrderNotificationSweep}from"../../../lib/order-notification-governance";

const json=(value:unknown,status=200)=>Response.json(value,{status,headers:{"cache-control":"no-store"}});

export async function GET(request:Request){try{const actor=await resolveActor(request),url=new URL(request.url),customerId=String(url.searchParams.get("customerId")||"").trim();if(!customerId)return json({error:"Customer ID is required"},400);const db=await database();await requireCustomerOwnership(db,actor,customerId);const sweep=await runOrderNotificationSweep(db,{actorId:actor.email}),items=await listOrderNotifications(db,customerId,Number(url.searchParams.get("limit")||50));return json({data:{items,unread:items.filter(item=>String(item.status)==="unread").length,sweep}})}catch(error){return authError(error,"Unable to load order notifications")}}

export async function POST(request:Request){try{const actor=await resolveActor(request),body=await request.json() as{customerId?:string;notificationId?:string;action?:string},customerId=String(body.customerId||"").trim(),notificationId=String(body.notificationId||"").trim();if(!customerId||!notificationId||body.action!=="mark_read")return json({error:"Customer, notification and mark_read action are required"},400);const db=await database();await requireCustomerOwnership(db,actor,customerId);return json({data:await markOrderNotificationRead(db,{customerId,notificationId})})}catch(error){return authError(error,"Unable to update order notification")}}
