import{authError,database}from"../../../lib/server-auth";
import{listBoardingHosts,listBoardingPackages,type BoardingPaymentMode}from"../../../lib/boarding-governance";
import{discoverBoardingHosts}from"../../../lib/boarding-host-discovery";
import{createLiveBoardingQuote}from"../../../lib/live-commercial-quotes";
const json=(value:unknown,status=200)=>Response.json(value,{status,headers:{"cache-control":"no-store"}});
function sameOriginWrite(request:Request){const origin=request.headers.get("origin");if(origin&&origin!==new URL(request.url).origin)throw new Response("Cross-origin Boarding quote blocked",{status:403});}
async function failure(error:unknown){if(error instanceof Response&&error.status>=400&&error.status<500){const message=await error.text().catch(()=>"");return json({error:message||"Boarding commercial request failed"},error.status);}return authError(error,"Boarding commercial request failed");}
const readiness={productionReady:false,paymentMode:"uat_sandbox",liveMoney:false};
export async function GET(request:Request){try{const url=new URL(request.url),cityId=String(url.searchParams.get("cityId")||"").trim(),zoneId=String(url.searchParams.get("zoneId")||"").trim(),scheduledStart=String(url.searchParams.get("scheduledStart")||"").trim(),scheduledEnd=String(url.searchParams.get("scheduledEnd")||"").trim(),petCount=Number(url.searchParams.get("petCount")||0),species=String(url.searchParams.get("species")||"").split(",").map(value=>value.trim()).filter(Boolean),db=await database(),hasLocation=Boolean(cityId&&zoneId),windowAware=Boolean(hasLocation&&scheduledStart&&scheduledEnd&&petCount&&species.length);const packages=await listBoardingPackages(db,scheduledStart||undefined),hosts=hasLocation?(windowAware?await discoverBoardingHosts(db,{cityId,zoneId,scheduledStart,scheduledEnd,petCount,species}):await listBoardingHosts(db,{cityId,zoneId,at:scheduledStart||undefined})):[];return json({data:{packages,hosts,source:"canonical_boarding_governance",availabilityMode:hasLocation?(windowAware?"uat_canonical":"catalogue_only"):"location_required",availabilityVerified:windowAware,liveAvailability:false,...readiness}});}catch(error){return failure(error);}}
export async function POST(request:Request){try{sameOriginWrite(request);const body=await request.json() as{packageCode?:string;petCount?:number;scheduledStart?:string;scheduledEnd?:string;paymentMode?:BoardingPaymentMode;couponCode?:string;cityId?:string;zoneId?:string};const packageCode=String(body.packageCode||"").trim(),scheduledStart=String(body.scheduledStart||"").trim(),scheduledEnd=String(body.scheduledEnd||"").trim(),paymentMode=body.paymentMode;if(!packageCode||!scheduledStart||!scheduledEnd||!paymentMode)return json({error:"Package, stay window and payment mode are required"},400);const db=await database(),quote=await createLiveBoardingQuote(db,{packageCode,petCount:Number(body.petCount||0),scheduledStart,scheduledEnd,paymentMode,couponCode:body.couponCode,cityId:body.cityId,zoneId:body.zoneId});return json({data:{...readiness,...quote}},201);}catch(error){return failure(error);}}
