"use client";
import Link from"next/link";
import{useCallback,useEffect,useState}from"react";

type Row=Record<string,unknown>;
type Snapshot={profiles:Row[];intents:Row[];knowledge:Row[];prompts:Row[];killSwitches:Row[];auditEvents:Row[];productionReady:boolean};
const card={background:"white",border:"1px solid #e5dcef",borderRadius:14,padding:14};
export default function AiConfigurationPage(){const[data,setData]=useState<Snapshot|null>(null),[error,setError]=useState(""),[busy,setBusy]=useState("");const load=useCallback(async()=>{const response=await fetch("/api/ai-business-configuration",{cache:"no-store"}),body=await response.json() as {data?:Snapshot;error?:string};if(!response.ok)throw new Error(body.error||"AI configuration unavailable");setData(body.data||null);},[]);useEffect(()=>{void load().catch(e=>setError(e instanceof Error?e.message:"AI configuration unavailable"));},[load]);async function transition(entityType:string,entityId:string,lifecycleAction:string){setBusy(entityId);setError("");try{const response=await fetch("/api/ai-business-configuration",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"transition",entityType,entityId,lifecycleAction})}),body=await response.json() as {error?:string};if(!response.ok)throw new Error(body.error||"Transition failed");await load();}catch(e){setError(e instanceof Error?e.message:"Transition failed")}finally{setBusy("")}}async function globalKill(disabled:boolean){setBusy("global");try{const response=await fetch("/api/ai-business-configuration",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"kill_switch",scopeType:"global",scopeKey:"ai",disabled,reason:disabled?"Manual staff safety stop":"Manual staff re-enable"})}),body=await response.json() as {error?:string};if(!response.ok)throw new Error(body.error||"Kill switch failed");await load();}catch(e){setError(e instanceof Error?e.message:"Kill switch failed")}finally{setBusy("")}}const section=(title:string,entityType:string,rows:Row[])=><section style={{...card,marginBottom:14}}><h2 style={{marginTop:0}}>{title}</h2>{rows.length===0?<p style={{color:"#746b7d"}}>No versions configured.</p>:rows.map(row=><article key={String(row.id)} style={{borderTop:"1px solid #eee6f5",padding:"10px 0",display:"grid",gridTemplateColumns:"2fr 1fr 2fr",gap:10,alignItems:"center"}}><div><b>{String(row.profile_key||row.intent_code||row.source_key||row.policy_key||row.id)}</b><small style={{display:"block"}}>v{String(row.version)} · {String(row.status)} · {String(row.immutable_hash||"").slice(0,12)}</small></div><span>{String(row.business_owner||row.title||row.brand_voice||"versioned policy")}</span><div>{row.status==="draft"&&<button disabled={busy===row.id} onClick={()=>transition(entityType,String(row.id),"submit_review")}>Submit review</button>} {row.status==="review"&&<button disabled={busy===row.id} onClick={()=>transition(entityType,String(row.id),"approve")}>Approve</button>} {row.status==="approved"&&<button disabled={busy===row.id} onClick={()=>transition(entityType,String(row.id),"activate")}>Activate</button>} {row.status==="retired"&&<button disabled={busy===row.id} onClick={()=>transition(entityType,String(row.id),"rollback")}>Rollback</button>}</div></article>)}</section>;
return <main style={{minHeight:"100vh",background:"#f7f4fb",padding:28,fontFamily:"Arial,sans-serif",color:"#24133f"}}><div style={{maxWidth:1300,margin:"0 auto"}}><header style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:18}}><div><small style={{fontWeight:800,color:"#6c39a8"}}>PAWSPACE TEAM · AI BUSINESS CONFIGURATION</small><h1 style={{margin:"7px 0"}}>Assistant configuration & knowledge</h1><p style={{margin:0,color:"#746b7d"}}>Versioned, reviewed and auditable AI business configuration. Production provider activation remains separate.</p></div><div><button disabled={busy==="global"} onClick={()=>globalKill(true)}>Disable AI</button> <button disabled={busy==="global"} onClick={()=>globalKill(false)}>Enable AI</button> <Link href="/team/ai" style={{marginLeft:10}}>AI review</Link></div></header>{error&&<div style={{padding:12,background:"#fff1f1",borderRadius:10,marginBottom:12}}>{error}</div>}{!data?<p>Loading…</p>:<>{section("Assistant profiles","profile",data.profiles)}{section("Intent catalogue","intent",data.intents)}{section("Approved knowledge registry","knowledge",data.knowledge)}{section("Prompt / system-policy versions","prompt",data.prompts)}<section style={card}><h2 style={{marginTop:0}}>Kill switches & audit</h2><p>Active switches: {data.killSwitches.filter(row=>Number(row.disabled)===1).length} · Audit events: {data.auditEvents.length}</p><p style={{fontSize:12,color:"#746b7d"}}>Configuration changes require settings.manage and are separately security-audited. Active versions are immutable snapshots; changes create new versions.</p></section></>}</div></main>}
