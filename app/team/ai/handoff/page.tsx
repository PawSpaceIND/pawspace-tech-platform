"use client";
import Link from"next/link";
import{useCallback,useEffect,useState}from"react";

type Thread={id:string;customer_id?:string;customer_name?:string;assigned_to?:string;status?:string;sla_due_at?:number};
type Handoff={current?:Record<string,unknown>|null;events?:Record<string,unknown>[];aiPaused?:boolean;sameCanonicalThread?:boolean};
const box={background:"white",border:"1px solid #e5dcef",borderRadius:14};
const label=(v:unknown)=>String(v||"—").replaceAll("_"," ");
export default function AiHandoffPage(){const[threads,setThreads]=useState<Thread[]>([]),[selected,setSelected]=useState<Thread|null>(null),[handoff,setHandoff]=useState<Handoff|null>(null),[error,setError]=useState(""),[busy,setBusy]=useState(false);
 const loadThreads=useCallback(async()=>{const r=await fetch("/api/conversations?status=open",{cache:"no-store"});const b=await r.json()as{data?:{threads:Thread[]};error?:string};if(!r.ok)throw new Error(b.error||"Unable to load conversations");const rows=b.data?.threads||[];setThreads(rows);return rows;},[]);
 const loadHandoff=useCallback(async(thread:Thread)=>{if(!thread.customer_id)return setHandoff(null);const r=await fetch(`/api/ai-human-handoff?threadId=${encodeURIComponent(thread.id)}&customerId=${encodeURIComponent(thread.customer_id)}`,{cache:"no-store"});const b=await r.json()as{data?:Handoff;error?:string};if(!r.ok)throw new Error(b.error||"Unable to load handoff");setHandoff(b.data||null);},[]);
 useEffect(()=>{void loadThreads().then(rows=>{if(rows[0])setSelected(rows[0])}).catch(e=>setError(e instanceof Error?e.message:"Unable to load conversations"))},[loadThreads]);
 useEffect(()=>{if(selected)void loadHandoff(selected).catch(e=>setError(e instanceof Error?e.message:"Unable to load handoff"))},[selected,loadHandoff]);
 async function act(action:"take_over"|"resume_ai"){if(!selected?.customer_id)return;setBusy(true);setError("");try{const reason=action==="take_over"?"Staff accepted AI escalation":"Staff reviewed the thread and explicitly returned it to AI";const r=await fetch("/api/ai-human-handoff",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action,threadId:selected.id,customerId:selected.customer_id,reason})});const b=await r.json()as{error?:string};if(!r.ok)throw new Error(b.error||"Handoff action failed");await Promise.all([loadThreads(),loadHandoff(selected)]);}catch(e){setError(e instanceof Error?e.message:"Handoff action failed")}finally{setBusy(false)}}
 const current=handoff?.current||null,summary=current&&typeof current.summary==="object"?current.summary as Record<string,unknown>:null,transcript=Array.isArray(summary?.transcript)?summary?.transcript as Record<string,unknown>[]:[];
 return <main style={{minHeight:"100vh",background:"#f7f4fb",padding:28,fontFamily:"Arial, sans-serif",color:"#24133f"}}><div style={{maxWidth:1400,margin:"0 auto"}}><header style={{display:"flex",justifyContent:"space-between",alignItems:"center",gap:16,marginBottom:20}}><div><small style={{fontWeight:800,color:"#6c39a8"}}>PAWSPACE TEAM · AI GATE 4</small><h1 style={{margin:"6px 0"}}>Human handoff & staff takeover</h1><p style={{margin:0,color:"#756c7d"}}>AI pauses during staff ownership. Return to AI requires an explicit governed staff action.</p></div><Link href="/team/customer-experience" style={{padding:10,textDecoration:"none",...box}}>CX workspace</Link></header>{error&&<div style={{padding:12,marginBottom:14,background:"#fff1f1",border:"1px solid #efc2c2",borderRadius:10}}>{error}</div>}<section style={{display:"grid",gridTemplateColumns:"minmax(320px,.8fr) minmax(540px,1.4fr)",gap:16}}><aside style={{...box,overflow:"hidden"}}><div style={{padding:16,borderBottom:"1px solid #eee6f5"}}><b>Open canonical threads</b></div>{threads.map(t=><button key={t.id} onClick={()=>setSelected(t)} style={{display:"block",width:"100%",textAlign:"left",padding:14,border:0,borderBottom:"1px solid #f0ebf4",background:selected?.id===t.id?"#f2ebfa":"white"}}><strong>{t.customer_name||t.customer_id||"Customer"}</strong><div style={{fontSize:12,marginTop:4}}>{label(t.assigned_to)} · {label(t.status)}</div></button>)}</aside><article style={{...box,padding:20,minHeight:560}}>{!selected?<p>Select a thread.</p>:<><div style={{display:"flex",justifyContent:"space-between",gap:12}}><div><small>{selected.id}</small><h2 style={{margin:"5px 0"}}>{selected.customer_name||selected.customer_id}</h2><p style={{margin:0}}>Assignment: <b>{label(selected.assigned_to)}</b></p></div><div><b>{handoff?.aiPaused?"AI paused":"AI available"}</b></div></div><hr style={{border:0,borderTop:"1px solid #eee6f5",margin:"18px 0"}}>{!current?<p>No Gate-4 handoff is active or recorded for this thread.</p>:<><p><b>Status:</b> {label(current.status)} · <b>Reason:</b> {label(current.reason)} · <b>Queue:</b> {label(current.queue_code)}</p><p><b>Confidence:</b> {current.confidence==null?"—":String(current.confidence)}</p><div style={{display:"flex",gap:8,margin:"14px 0"}}><button disabled={busy||String(current.status)!=="queued"} onClick={()=>act("take_over")}>Take over</button><button disabled={busy||String(current.status)!=="staff_active"} onClick={()=>act("resume_ai")}>Return to AI</button></div><h3>Handoff transcript summary</h3>{transcript.length===0?<p style={{color:"#756c7d"}}>No transcript excerpt captured.</p>:transcript.map((m,i)=><div key={i} style={{padding:10,marginBottom:8,background:"#f6f5f7",borderRadius:10}}><small>{label(m.direction)} · {label(m.channel)}</small><div>{String(m.text||"")}</div></div>)}</>}</>}</article></section></div></main>}
