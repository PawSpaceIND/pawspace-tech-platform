"use client";
import{useEffect,useState}from"react";
import Link from"next/link";

type Mapping={source_key:string;account_code:string;approval_reference:string};
type ExpenseLink={expense_id:string;employee_id:string;employee_code?:string|null;merchant?:string|null;amount?:number|null;linkage_status:string};
type PayrollPost={payroll_run_id:string;period_code:string;status:string;total_debit:number;total_credit:number};
type Policy={id:string;policy_code:string;version:number;status:string};
type ExportRow={id:string;payroll_run_id:string;period_code:string;sandbox_only:number;external_submission:number};
type Reconciliation={id:string;payroll_batch_id:string;period_code:string;sandbox_reference:string;expected_amount:number;matched_amount:number;status:string};
type Period={period_code:string;status:string};
type Payload={mappings:Mapping[];expenseLinks:ExpenseLink[];payrollPosts:PayrollPost[];statutoryPolicies:Policy[];statutoryExports:ExportRow[];bankReconciliations:Reconciliation[];periods:Period[];requiredPayrollAccountKeys:string[];truth:{expenseEmployeeLinkageEnabled:boolean;payrollJournalConfigured:boolean;missingPayrollAccountMappings:string[];statutoryPolicyConfigured:boolean;financePeriodLockingEnforced:boolean;statutoryExternalSubmissionEnabled:boolean;liveBankTransmissionEnabled:boolean;bankReconciliationMode:string;sandboxOnly:boolean;productionReady:boolean}};
async function loadPayload(){const r=await fetch("/api/people-finance",{cache:"no-store"}),p=await r.json();if(!r.ok)throw new Error(p.error||"People Finance load failed");return p.data as Payload;}
const rupee=(v:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:2}).format(v||0);
export default function PeopleFinancePage(){const[data,setData]=useState<Payload|null>(null),[error,setError]=useState(""),[loading,setLoading]=useState(true);useEffect(()=>{let active=true;void loadPayload().then(x=>{if(active){setData(x);setError("");}}).catch(e=>{if(active)setError(e instanceof Error?e.message:String(e));}).finally(()=>{if(active)setLoading(false);});return()=>{active=false;};},[]);return <main style={{maxWidth:1180,margin:"0 auto",padding:"32px 20px",fontFamily:"system-ui,sans-serif"}}><p><Link href="/team/people">← People</Link> · <Link href="/team/people/payroll">Payroll →</Link></p><p style={{fontWeight:800,letterSpacing:1}}>PAWSPACE · PEOPLE · FINANCE + STATUTORY</p><h1>Gate 5 integration control</h1><p>Employee expenses can be linked to the employee master. Approved payroll can post only through Finance-approved account mappings and unlocked periods. Statutory output and bank reconciliation are UAT sandbox records only; this workspace never submits a filing or transmits a bank instruction.</p>{error?<p>{error}</p>:null}<section style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(210px,1fr))",gap:10,margin:"18px 0"}}><article style={{border:"1px solid #dcece5",borderRadius:12,padding:14}}><small>Expense links</small><strong style={{display:"block",fontSize:28}}>{data?.expenseLinks.length||0}</strong></article><article style={{border:"1px solid #dcece5",borderRadius:12,padding:14}}><small>Payroll journals</small><strong style={{display:"block",fontSize:28}}>{data?.payrollPosts.length||0}</strong></article><article style={{border:"1px solid #dcece5",borderRadius:12,padding:14}}><small>Statutory policies</small><strong style={{display:"block",fontSize:28}}>{data?.statutoryPolicies.length||0}</strong></article><article style={{border:"1px solid #dcece5",borderRadius:12,padding:14}}><small>Bank mode</small><strong style={{display:"block",fontSize:18}}>{data?.truth.bankReconciliationMode||"sandbox_reference_only"}</strong></article></section><h2>Payroll account mapping boundary</h2><section style={{display:"grid",gap:8}}>{data?.requiredPayrollAccountKeys.map(key=>{const mapping=data.mappings.find(x=>x.source_key===key);return <article key={key} style={{border:"1px solid #dcece5",borderRadius:10,padding:12}}><b>{key}</b><div>{mapping?<>Account <code>{mapping.account_code}</code> · approval {mapping.approval_reference}</>:"Configuration required before Finance posting"}</div></article>;})}</section><h2>Recent payroll Finance posts</h2><section style={{display:"grid",gap:8}}>{data?.payrollPosts.map(post=><article key={post.payroll_run_id} style={{border:"1px solid #dcece5",borderRadius:10,padding:12}}><b>{post.period_code} · {post.status}</b><div><code>{post.payroll_run_id}</code></div><div>Debit {rupee(post.total_debit)} · Credit {rupee(post.total_credit)}</div></article>)}</section><h2>Sandbox reconciliation references</h2><section style={{display:"grid",gap:8}}>{data?.bankReconciliations.map(row=><article key={row.id} style={{border:"1px solid #dcece5",borderRadius:10,padding:12}}><b>{row.period_code} · {row.status}</b><div>{row.sandbox_reference}</div><div>Expected {rupee(row.expected_amount)} · matched {rupee(row.matched_amount)}</div></article>)}</section>{loading?<p>Loading Gate 5 controls…</p>:null}<footer style={{marginTop:24}}><b>Gate 5 engineering:</b> IMPLEMENTED FOR UAT · <b>External statutory submission:</b> NO · <b>Live bank transmission:</b> NO · <b>Production ready:</b> NO</footer></main>}
