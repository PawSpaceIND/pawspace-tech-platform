"use client";
import{useEffect,useState}from"react";
import Link from"next/link";
type Team={teamCode:string;headcount:number;attendanceExceptions:number;payrollCost:number};type Metric={code:string;source:string;calculation:string};type PayrollRow={result_id:string;run_id:string;employee_id:string;net_pay:number;run_status:string};type Payload={period:{startDate:string;endDate:string};scope:{mode:string;employeeCount:number};metricDefinitions:Metric[];headcount:{active:number;inactive:number;joiners:number;leavers:number;total:number};teamRollups:Team[];attendance:{available:boolean;exceptions:number;byStatus:Record<string,number>;rows:{id:string;employee_id:string;work_date:string;status:string;exception_code?:string|null}[]};payroll:{available:boolean;register:PayrollRow[];variance:{netPayChange:number;grossChange:number}|null;costByCostCentre:{costCentre:string;amount:number}[]};incentives:{available:boolean;byStatus:Record<string,number>;approvedAmount:number};finance:{available:boolean;expenseLinks:{expense_id:string;employee_id:string;amount:number;status:string}[];expenseAmount:number;statutoryExports:{id:string;period_code:string;external_submission:number}[];statutoryExternalSubmissionEnabled:boolean;liveBankTransmissionEnabled:boolean};audit:{available:boolean;peopleEvents:unknown[];securityEvents:unknown[]};truth:{managerScopeEnforced:boolean;sensitivePayrollPermissionGated:boolean;sensitiveIncentivePermissionGated:boolean;staticDemoCountersAccepted:boolean;sourceDrilldownEnabled:boolean;productionReady:boolean}};
const money=(v:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:2}).format(v||0);async function load(){const r=await fetch("/api/people-reports",{cache:"no-store"}),p=await r.json();if(!r.ok)throw new Error(p.error||"People reports load failed");return p.data as Payload;}
export default function PeopleReportsPage(){const[data,setData]=useState<Payload|null>(null),[error,setError]=useState(""),[loading,setLoading]=useState(true);useEffect(()=>{let active=true;void load().then(x=>{if(active)setData(x);}).catch(e=>{if(active)setError(e instanceof Error?e.message:String(e));}).finally(()=>{if(active)setLoading(false);});return()=>{active=false;};},[]);return <main style={{maxWidth:1180,margin:"0 auto",padding:"32px 20px",fontFamily:"system-ui,sans-serif"}}><p><Link href="/team/people">← People</Link></p><p style={{fontWeight:800,letterSpacing:1}}>PAWSPACE · PEOPLE · REPORTS</p><h1>Gate 6 productivity and audit reports</h1><p>Source-derived People reporting with manager scope, permission-gated payroll/incentive sections and record-ID drill-down. These reports do not become payroll, incentive or disciplinary authority.</p>{error?<p>{error}</p>:null}<section style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(190px,1fr))",gap:10,margin:"18px 0"}}><article style={{border:"1px solid #ddd",borderRadius:12,padding:14}}><small>Active headcount</small><strong style={{display:"block",fontSize:28}}>{data?.headcount.active||0}</strong></article><article style={{border:"1px solid #ddd",borderRadius:12,padding:14}}><small>Joiners</small><strong style={{display:"block",fontSize:28}}>{data?.headcount.joiners||0}</strong></article><article style={{border:"1px solid #ddd",borderRadius:12,padding:14}}><small>Leavers</small><strong style={{display:"block",fontSize:28}}>{data?.headcount.leavers||0}</strong></article><article style={{border:"1px solid #ddd",borderRadius:12,padding:14}}><small>Report scope</small><strong style={{display:"block",fontSize:18}}>{data?.scope.mode||"—"} · {data?.scope.employeeCount||0}</strong></article></section><h2>Team rollups</h2><section style={{display:"grid",gap:8}}>{data?.teamRollups.map(t=><article key={t.teamCode} style={{border:"1px solid #ddd",borderRadius:10,padding:12}}><b>{t.teamCode}</b><div>Headcount {t.headcount} · attendance exceptions {t.attendanceExceptions}{data.payroll.available?<> · payroll cost {money(t.payrollCost)}</>:null}</div></article>)}</section><h2>Attendance exceptions</h2>{data?.attendance.available?<section style={{display:"grid",gap:8}}>{data.attendance.rows.filter(r=>r.exception_code).slice(0,20).map(r=><article key={r.id} style={{border:"1px solid #ddd",borderRadius:10,padding:12}}><b>{r.work_date} · {r.status}</b><div><code>{r.id}</code> · employee <code>{r.employee_id}</code> · {r.exception_code}</div></article>)}</section>:<p>Attendance report permission not granted.</p>}<h2>Payroll register</h2>{data?.payroll.available?<><p>{data.payroll.variance?<>Latest net-pay variance: {money(data.payroll.variance.netPayChange)}</>:"A prior run is required for variance."}</p><section style={{display:"grid",gap:8}}>{data.payroll.register.slice(0,20).map(r=><article key={r.result_id} style={{border:"1px solid #ddd",borderRadius:10,padding:12}}><b>{r.run_status} · {money(r.net_pay)}</b><div>result <code>{r.result_id}</code> · run <code>{r.run_id}</code> · employee <code>{r.employee_id}</code></div></article>)}</section></>:<p>Payroll report permission not granted; compensation stays hidden.</p>}<h2>Incentives</h2>{data?.incentives.available?<p>Approved amount {money(data.incentives.approvedAmount)} · status counts {JSON.stringify(data.incentives.byStatus)}</p>:<p>Incentive report permission not granted.</p>}<h2>Finance + statutory boundary</h2>{data?.finance.available?<p>Linked expense amount {money(data.finance.expenseAmount)} · statutory sandbox exports {data.finance.statutoryExports.length} · external submission NO · live bank NO</p>:<p>Finance report permission not granted.</p>}<h2>Metric definitions</h2><section style={{display:"grid",gap:8}}>{data?.metricDefinitions.map(m=><article key={m.code} style={{border:"1px solid #ddd",borderRadius:10,padding:12}}><b>{m.code}</b><div>Source: <code>{m.source}</code></div><div>{m.calculation}</div></article>)}</section>{loading?<p>Loading Gate 6 reports…</p>:null}<footer style={{marginTop:24}}><b>Gate 6 engineering:</b> IMPLEMENTED FOR UAT · <b>Manager scope:</b> ENFORCED · <b>Static/demo performance truth:</b> NO · <b>Production ready:</b> NO</footer></main>}
