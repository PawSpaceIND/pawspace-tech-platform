"use client";
import{useEffect,useState}from"react";
import Link from"next/link";
type Run={id:string;period_start:number;period_end:number;status:string;created_by:string;reviewed_by?:string|null;approved_by?:string|null};
type Structure={id:string;structure_code:string;version:number;status:string;effective_from:number};
type Payload={runs:Run[];structures:Structure[];truth:{statutoryPolicyConfigured:boolean;incentivePolicyConfigured:boolean;bankTransmissionEnabled:boolean;approvedRunImmutable:boolean;productionReady:boolean}};
async function loadPayload(){const r=await fetch("/api/payroll",{cache:"no-store"}),p=await r.json();if(!r.ok)throw new Error(p.error||"Payroll load failed");return p.data as Payload;}
const day=(v:number)=>new Date(v).toLocaleDateString("en-IN",{timeZone:"Asia/Kolkata"});
export default function PayrollPage(){const[data,setData]=useState<Payload|null>(null),[error,setError]=useState(""),[loading,setLoading]=useState(true);useEffect(()=>{let active=true;void loadPayload().then(x=>{if(active){setData(x);setError("");}}).catch(e=>{if(active)setError(e instanceof Error?e.message:String(e));}).finally(()=>{if(active)setLoading(false);});return()=>{active=false;};},[]);return <main style={{maxWidth:1180,margin:"0 auto",padding:"32px 20px",fontFamily:"system-ui,sans-serif"}}><p><Link href="/team/people">← People</Link> · <Link href="/team/people/finance">Finance + Statutory →</Link></p><p style={{fontWeight:800,letterSpacing:1}}>PAWSPACE · PEOPLE · PAYROLL</p><h1>Deterministic payroll control</h1><p>Salary structures and employee compensation are effective-dated. Payroll calculations snapshot configured components and approved source facts. Statutory policy is explicit configuration, Finance journal posting requires approved account mappings, and payment preparation remains sandbox-only.</p>{error?<p>{error}</p>:null}<section style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))",gap:10,margin:"18px 0"}}><article style={{border:"1px solid #dcece5",borderRadius:12,padding:14}}><small>Salary structures</small><strong style={{display:"block",fontSize:28}}>{data?.structures.length||0}</strong></article><article style={{border:"1px solid #dcece5",borderRadius:12,padding:14}}><small>Payroll runs</small><strong style={{display:"block",fontSize:28}}>{data?.runs.length||0}</strong></article><article style={{border:"1px solid #dcece5",borderRadius:12,padding:14}}><small>Bank transmission</small><strong style={{display:"block",fontSize:20}}>{data?.truth.bankTransmissionEnabled?"ENABLED":"SANDBOX ONLY"}</strong></article></section><h2>Payroll runs</h2><section style={{display:"grid",gap:8}}>{data?.runs.map(r=><article key={r.id} style={{border:"1px solid #dcece5",borderRadius:10,padding:12}}><b>{day(r.period_start)} → {day(r.period_end)} · {r.status}</b><div><code>{r.id}</code></div><div>Maker: {r.created_by} · Reviewer: {r.reviewed_by||"—"} · Approver: {r.approved_by||"—"}</div></article>)}</section>{loading?<p>Loading payroll…</p>:null}<footer style={{marginTop:24}}><b>Statutory policy:</b> CONFIGURATION REQUIRED · <b>Finance journal:</b> CONFIGURATION-GATED · <b>Live bank instruction:</b> NO · <b>Production ready:</b> NO</footer></main>}
