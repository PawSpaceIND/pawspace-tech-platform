"use client";
import{useEffect,useState}from"react";
import Link from"next/link";
type AttendanceDay={id:string;employee_id:string;work_date:string;status:string;first_check_in?:number|null;last_check_out?:number|null;worked_minutes?:number|null;exception_code?:string|null};
type LeaveRequest={id:string;employee_id:string;leave_code:string;start_date:string;end_date:string;units:number;status:string;reason:string};
type Adjustment={id:string;employee_id:string;work_date:string;status:string;reason:string};
type Payload={attendanceDays:AttendanceDay[];pendingAdjustments:Adjustment[];leaveRequests:LeaveRequest[];truth:{hardcodedGraceMinutes:boolean;hardcodedLeaveEntitlement:boolean;hardcodedOvertimeRate:boolean;gpsRequired:boolean;productionReady:boolean}};
const when=(v?:number|null)=>v?new Date(v).toLocaleString("en-IN",{timeZone:"Asia/Kolkata"}):"—";
async function fetchPayload(){const r=await fetch("/api/attendance-leave",{cache:"no-store"}),p=await r.json();if(!r.ok)throw new Error(p.error||"Attendance load failed");return p.data as Payload;}
export default function PeopleTimePage(){const[data,setData]=useState<Payload|null>(null),[error,setError]=useState(""),[loading,setLoading]=useState(true);async function load(){setLoading(true);try{setData(await fetchPayload());setError("");}catch(e){setError(e instanceof Error?e.message:String(e));}finally{setLoading(false);}}useEffect(()=>{let active=true;void fetchPayload().then(payload=>{if(active){setData(payload);setError("");}}).catch(e=>{if(active)setError(e instanceof Error?e.message:String(e));}).finally(()=>{if(active)setLoading(false);});return()=>{active=false;};},[]);return <main style={{maxWidth:1180,margin:"0 auto",padding:"32px 20px",fontFamily:"system-ui,sans-serif"}}><p><Link href="/team/people">← People</Link></p><p style={{fontWeight:800,letterSpacing:1}}>PAWSPACE · PEOPLE · TIME & LEAVE</p><h1>Attendance, adjustments and leave</h1><p>Policy-driven attendance and leave truth. Grace periods, entitlements, overtime and location requirements stay configuration-required until approved.</p>{error?<p>{error}</p>:null}<section style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))",gap:10,margin:"18px 0"}}><article style={{border:"1px solid #ddd",borderRadius:12,padding:14}}><small>Attendance rows</small><strong style={{display:"block",fontSize:28}}>{data?.attendanceDays.length||0}</strong></article><article style={{border:"1px solid #ddd",borderRadius:12,padding:14}}><small>Pending adjustments</small><strong style={{display:"block",fontSize:28}}>{data?.pendingAdjustments.length||0}</strong></article><article style={{border:"1px solid #ddd",borderRadius:12,padding:14}}><small>Leave requests</small><strong style={{display:"block",fontSize:28}}>{data?.leaveRequests.length||0}</strong></article></section><p><button onClick={()=>void load()} disabled={loading}>{loading?"Refreshing…":"Refresh"}</button></p><h2>Attendance</h2><section style={{display:"grid",gap:8}}>{data?.attendanceDays.map(r=><article key={r.id} style={{border:"1px solid #ddd",borderRadius:10,padding:12}}><b>{r.work_date} · {r.status}</b><div><code>{r.employee_id}</code> · In {when(r.first_check_in)} · Out {when(r.last_check_out)} · Worked {r.worked_minutes??"—"} min</div>{r.exception_code?<div><b>Exception:</b> {r.exception_code}</div>:null}</article>)}</section><h2>Leave</h2><section style={{display:"grid",gap:8}}>{data?.leaveRequests.map(r=><article key={r.id} style={{border:"1px solid #ddd",borderRadius:10,padding:12}}><b>{r.leave_code} · {r.status}</b><div><code>{r.employee_id}</code> · {r.start_date} → {r.end_date} · {r.units} unit(s)</div><div>{r.reason}</div></article>)}</section>{loading&&!data?<p>Loading attendance and leave…</p>:null}<footer style={{marginTop:24}}><b>GPS required by code:</b> NO · <b>Hard-coded leave/grace/OT policy:</b> NO · <b>Production ready:</b> NO</footer></main>}
