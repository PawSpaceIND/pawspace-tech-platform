"use client";
import Link from"next/link";
import{useEffect,useState}from"react";
type Data=Record<string,any>;const card={background:"white",border:"1px solid #e5dcef",borderRadius:14,padding:16};const money=(n:unknown)=>`₹${Number(n||0).toLocaleString("en-IN")}`;
export default function AnalyticsPage(){const[data,setData]=useState<Data|null>(null),[error,setError]=useState("");useEffect(()=>{let on=true;fetch("/api/company-analytics",{cache:"no-store"}).then(async r=>{const b=await r.json();if(!r.ok)throw new Error(b.error||"Analytics unavailable");if(on)setData(b.data)}).catch(e=>{if(on)setError(e instanceof Error?e.message:"Analytics unavailable")});return()=>{on=false}},[]);return <main style={{minHeight:"100vh",background:"#f7f4fb",padding:28,fontFamily:"Arial,sans-serif",color:"#24133f"}}><div style={{maxWidth:1400,margin:"0 auto"}}><header style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:18}}><div><small style={{fontWeight:800,color:"#6c39a8"}}>PAWSPACE TEAM · ANALYTICS</small><h1 style={{margin:"7px 0"}}>Canonical company metric layer</h1><p style={{margin:0,color:"#746b7d"}}>No static operational counters. Metrics come from canonical booking, payment, CX and provider facts.</p></div><Link href="/team" style={{padding:10,background:"#4b168c",color:"white",borderRadius:10,textDecoration:"none"}}>Team home</Link></header>{error&&<div style={{padding:12,background:"#fff1f1",borderRadius:10}}>{error}</div>}{data&&<><section style={{display:"grid",gridTemplateColumns:"repeat(6,1fr)",gap:10,marginBottom:12}}>{[["Bookings",data.bookings.total],["Completed",data.bookings.completed],["Cancelled",data.bookings.cancelled],["GMV",money(data.money.gmv)],["Collected",money(data.money.collected)],["Customers",data.customers.unique]].map(([k,v])=><article key={String(k)} style={card}><small>{k}</small><strong style={{display:"block",fontSize:23,marginTop:6}}>{v}</strong></article>)}</section><section style={{display:"grid",gridTemplateColumns:"2fr 1fr",gap:12}}><article style={card}><h3>Service facts</h3>{Object.entries(data.services||{}).length===0?<p>No canonical bookings in the selected period.</p>:Object.entries(data.services).map(([code,v]:any)=><div key={code} style={{display:"grid",gridTemplateColumns:"2fr repeat(4,1fr)",padding:"9px 0",borderBottom:"1px solid #eee6f5"}}><b>{code}</b><span>{v.bookings} bookings</span><span>{v.completed} complete</span><span>{money(v.gmv)} GMV</span><span>{money(v.collected)} collected</span></div>)}</article><aside style={card}><h3>Data/source health</h3>{Object.entries(data.sourceStatus||{}).map(([k,v])=><div key={k} style={{padding:"7px 0",borderBottom:"1px solid #eee6f5"}}><b>{k}</b><small style={{display:"block"}}>{String(v)}</small></div>)}<h4>Quality</h4>{Object.entries(data.dataQuality||{}).map(([k,v])=><div key={k}>{k}: <b>{String(v)}</b></div>)}</aside></section><section style={{display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:10,marginTop:12}}><article style={card}><b>Customer repeat</b><strong style={{display:"block",fontSize:22,marginTop:6}}>{data.customers.repeatRate==null?"—":`${(Number(data.customers.repeatRate)*100).toFixed(1)}%`}</strong></article><article style={card}><b>Open CX tickets</b><strong style={{display:"block",fontSize:22,marginTop:6}}>{data.cx.open}</strong></article><article style={card}><b>Active providers</b><strong style={{display:"block",fontSize:22,marginTop:6}}>{data.providers.active}</strong></article></section><p style={{fontSize:12,color:"#746b7d"}}>Contribution margin, complete refund aggregation, marketing spend, tax and payout metrics remain unavailable until their approved canonical sources are connected.</p></>}</div></main>}
