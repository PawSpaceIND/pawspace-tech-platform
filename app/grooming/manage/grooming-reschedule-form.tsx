"use client";
import{useRef,useState}from"react";
import{changeGroomingBooking,type GroomingChangePreview}from"../../../lib/grooming-booking-change-client";
import styles from"./grooming-customer-booking.module.css";
export default function GroomingRescheduleForm({preview,customerId,onChanged,onRefresh}:{preview:GroomingChangePreview;customerId:string;onChanged:(message:string)=>void;onRefresh:()=>void}){
 const[date,setDate]=useState(""),[time,setTime]=useState(""),[reason,setReason]=useState(""),[accepted,setAccepted]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState(""),[needsRefresh,setNeedsRefresh]=useState(false);const sending=useRef(false);
 if(!preview.reschedule.allowed)return null;
 const submit=async(event:React.FormEvent)=>{event.preventDefault();if(sending.current||needsRefresh||!accepted||reason.trim().length<8||!date||!time)return;const start=new Date(`${date}T${time}:00+05:30`);if(!Number.isFinite(start.getTime())||start.getTime()<=Date.now()){setError("Choose a future date and time in IST.");return;}const end=new Date(start.getTime()+preview.durationMinutes*60000);sending.current=true;setBusy(true);setError("");
  try{const result=await changeGroomingBooking({bookingId:preview.bookingId,customerId,action:"reschedule",reason:reason.trim(),scheduledStart:start.toISOString(),scheduledEnd:end.toISOString(),expectedConsentRevision:preview.consentRevision});if(result.bookingId!==preview.bookingId||result.scheduledStart!==start.toISOString()||result.scheduledEnd!==end.toISOString())throw new Error("The new schedule could not be verified. Refresh your booking before trying again.");onChanged("Booking rescheduled. Your updated appointment is shown below.");}
  catch(problem){setError(problem instanceof Error?problem.message:"Unable to confirm the new time. Refresh before trying again.");setAccepted(false);setNeedsRefresh(true);}finally{sending.current=false;setBusy(false);}
 };
 return <form className={styles.cancelForm} aria-label="Reschedule booking" onSubmit={submit}><h3>Choose a new appointment</h3><p>Your service duration stays {preview.durationMinutes} minutes. All times are India Standard Time (IST).</p><><label>New appointment date<input type="date" required value={date} disabled={busy||needsRefresh} onChange={event=>{setDate(event.target.value);setAccepted(false);}}/></label><label>New appointment time (IST)<input type="time" required step={900} value={time} disabled={busy||needsRefresh} onChange={event=>{setTime(event.target.value);setAccepted(false);}}/></label><label>Reason for changing the appointment<textarea required minLength={8} maxLength={1000} value={reason} disabled={busy||needsRefresh} onChange={event=>setReason(event.target.value)}/></label><label className={styles.consent}><input type="checkbox" checked={accepted} disabled={busy||needsRefresh} onChange={event=>setAccepted(event.target.checked)}/><span>I accept the displayed terms and request this new time, subject to provider availability.</span></label>{error&&<p role="alert">{error}</p>}{needsRefresh?<button type="button" onClick={onRefresh}>Refresh rescheduling terms</button>:<button type="submit" disabled={busy||!accepted||!date||!time||reason.trim().length<8}>{busy?"Checking and updating…":"Confirm new appointment"}</button>}</></form>;
}
