name: Training Gate 3 final patch
on:
  push:
    branches: [grooming-integration-closure]
    paths:
      - .github/workflows/training-gate3-finish.yml
permissions:
  contents: write
jobs:
  finish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: grooming-integration-closure
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          cache: npm
      - run: python3 scripts/training-gate3-safety-fix.py
      - run: npm ci
      - run: npm run build
      - run: npm run lint
      - run: npm run validate:artifact
      - name: Commit verified Training Gate 3 safety fixes
        run: |
          git config user.name "PawSpaceIND"
          git config user.email "karthik@pawspace.in"
          git add lib/training-finance.ts lib/training-cancellation.ts
          git diff --cached --quiet && exit 0
          git commit -m "Cover trainer earnings by receipts and block in-flight cancellation [skip ci]"
          git pull --rebase origin grooming-integration-closure
          git push origin grooming-integration-closure
